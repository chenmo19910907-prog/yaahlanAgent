## 已录入 Admin 清单（自动生成）

> 本文件由 `Admin/scripts/generate_index.py` 根据 `Admin/config/registry.json` 自动生成，请勿手动编辑。

### 目录

- [1) 公会（melon-gateway / cms/anchor）](#admin-cat-1)
  - [公会-查询信息](#query_guild)
  - [公会-用户加入](#add_guild_member)
  - [公会-用户移除](#remove_guild_member)
  - [公会-用户转移](#change_guild_member)
- [2) 定制座驾（yaahlan-admin）](#admin-cat-2)
  - [定制座驾-重置上传冷却](#reset_custom_vehicle_cooldown)
- [3) 定制礼物（melon-gateway）](#admin-cat-3)
  - [定制礼物-查询列表](#query_custom_gift_list)
- [4) 定制礼物（yaahlan-admin）](#admin-cat-4)
  - [定制礼物-重置上传时间](#reset_custom_gift_upload)
- [5) 定制装扮工作流（MOA + Admin）](#admin-cat-5)
  - [定制头像框-开通流程](#setup_custom_avatar_frame)
  - [定制座驾-开通流程](#setup_custom_vehicle)
- [6) 定制道具（yaahlan-admin）](#admin-cat-6)
  - [定制头像框-重置上传冷却](#reset_custom_prop_cooldown)
- [7) 客服（melon-gateway / cms/customerservice）](#admin-cat-7)
  - [客服-修改接单状态](#change_cs_taking_order)
  - [客服-新增/编辑账号](#save_cs_data)
  - [客服-查询账号列表](#query_cs_data)
- [8) 家族（melon-gateway）](#admin-cat-8)
  - [家族-增加成员](#add_family_member)
  - [家族-查询信息](#query_family)
  - [家族-查询全部列表](#list_all_families)
- [9) 版本（melon-gateway / backend/pangu）](#admin-cat-9)
  - [版本-查询 App Store 审核版本](#query_app_store_review_version)
  - [版本-设置 App Store 审核版本](#update_app_store_review_version)
- [10) 用户（mdp-nova / userAdmin + MOA）](#admin-cat-10)
  - [用户-批量互关结好友](#batch_mutual_friends_from_user_list)
- [11) 用户（mdp-nova / userAdmin）](#admin-cat-11)
  - [用户-查询列表](#query_user_profile_list)
  - [用户-注销账号](#cancel_user)
- [12) 用户（yaahlan-admin）](#admin-cat-12)
  - [用户-查询动态列表](#query_user_feed_list)
  - [用户-查询历史登录设备](#query_user_history_device_list)
  - [用户-查询设备历史登录账号](#query_history_user_list_by_device_id)
  - [用户-查询详情](#query_user_detail)
  - [设备-查询当前登录账号](#query_device_current_account)
- [13) 礼物（mdp-nova / giftAdmin）](#admin-cat-13)
  - [礼物-查询列表](#query_gift_list)
- [14) 道具（mdp-nova / propAdmin）](#admin-cat-14)
  - [道具-查询配置列表](#query_prop_info)

### 使用说明

- **提示词**：你对我说的自然语言口令
- **命令**：对应可执行脚本命令（默认已配置 `Admin/.env.local`）
- **线上环境**：见 `online/` 模块（`python3 online/online_execute.py`）；本目录仅测试环境
- **鉴权**：请求头 `sso-token`、`yaahlan-jwt`（从 Yaahlan 测试后台抓包）
- **完整响应**：追加 `--output json`
- **定制装扮**：定制头像框需 **VIP6**，定制座驾需 **VIP8**；先 MOA 升级，再 Admin 重置上传冷却（见「定制装扮工作流」）

<a id="admin-cat-1"></a>

## 1) 公会（melon-gateway / cms/anchor）

<a id="query_guild"></a>

### 公会-查询信息

- **功能**：按公会长 userId、公会 ID 或名称查询公会信息（tradeUnionPageList；含母公会、子公会、提现方式、主播人数、分成比例等）
- **提示词**：
  - `查询公会长 <userId> 的公会信息`
  - `查公会 <tradeUnion> 详情`
  - `公会 ID <tradeId> 有多少主播`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --query-guild \
  --guild-leader-id <userId>
```

<a id="add_guild_member"></a>

### 公会-用户加入

- **功能**：把用户加入指定公会（addAnchor；params=tradeId 或 tradeUnion、userIds）。userIds 可逗号分隔多个用户
- **提示词**：
  - `用户 <userId> 加入公会 <tradeId>`
  - `把 <userId> 加入公会 <tradeUnion>`
  - `公会 <tradeId> 添加成员 <userId>`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --add-guild-member \
  --trade-id <tradeId> \
  --guild-user-id <userId>
```

<a id="remove_guild_member"></a>

### 公会-用户移除

- **功能**：把用户从公会移除（batchDeleteAnchor；params=anchorIdList）。可逗号分隔多个 userId
- **提示词**：
  - `用户 <userId> 移除公会`
  - `把 <userId> 踢出公会`
  - `移除公会成员 <userId>`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --remove-guild-member \
  --guild-user-id <userId>
```

<a id="change_guild_member"></a>

### 公会-用户转移

- **功能**：把用户转移到目标公会（batchAnchorChangeTradeUnion；params=tradeUnion、userIdSet）。userIdSet 可逗号分隔多个 userId
- **提示词**：
  - `用户 <userId> 转移到公会 <tradeUnion>`
  - `把 <userId> 转到公会 <tradeUnion>`
  - `<userId> 加入公会 <tradeUnion>（已在其他公会时转移）`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --change-guild-member \
  --trade-union <tradeUnion> \
  --guild-user-id <userId>
```

<a id="admin-cat-2"></a>

## 2) 定制座驾（yaahlan-admin）

<a id="reset_custom_vehicle_cooldown"></a>

### 定制座驾-重置上传冷却

- **功能**：重置用户定制座驾上传冷却时间（resetCoolDown；params=remoteId，即 userId）。需 VIP8 才可定制座驾；用户座驾审核中或当前可定制时不支持重置
- **提示词**：
  - `重置用户 <userId> 定制座驾上传时间`
  - `重置 <userId> 自定义座驾上传冷却`
  - `清除 <userId> 定制座驾 cooldown`
  - `用户 <userId> 需要定制座驾，先升 VIP8 再重置座驾上传冷却`
  - `<userId> 需要定制座驾`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --reset-custom-vehicle-cooldown \
  --custom-vehicle-remote-id <userId>
```

<a id="admin-cat-3"></a>

## 3) 定制礼物（melon-gateway）

<a id="query_custom_gift_list"></a>

### 定制礼物-查询列表

- **功能**：查询 VIP5 定制礼物 userId ↔ giftId 映射（getListConfig）
- **提示词**：
  - `查询定制礼物列表`
  - `用户 <userId> 的定制礼物 id 是多少`
  - `查 <userId> 自定义礼物 giftId`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-custom-gift-list --custom-gift-user-id <userId>
```

<a id="admin-cat-4"></a>

## 4) 定制礼物（yaahlan-admin）

<a id="reset_custom_gift_upload"></a>

### 定制礼物-重置上传时间

- **功能**：重置用户定制礼物上传次数/过期时间（resetExpireTime；params=userId）。与 MOA resetExpireTime 等效，走 Yaahlan 测试后台
- **提示词**：
  - `重置用户 <userId> 定制礼物上传次数`
  - `重置 <userId> 自定义礼物上传时间`
  - `清除 <userId> 定制礼物上传冷却`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --reset-custom-gift-upload \
  --custom-gift-reset-user-id <userId>
```

<a id="admin-cat-5"></a>

## 5) 定制装扮工作流（MOA + Admin）

<a id="setup_custom_avatar_frame"></a>

### 定制头像框-开通流程

- **功能**：定制头像框需 VIP6。按顺序执行：① MOA 升到 VIP6；② Admin 重置 HEADER_FRAME 上传冷却（resetCoolDownProp）
- **提示词**：
  - `用户 <userId> 需要定制头像框`
  - `<userId> 需要定制头像框，先升级 VIP6 再重置上传`
  - `100465989 需要定制头像框`
- **命令**：

```bash
# ① 升到 VIP6（MOA）
python3 MOA/moa_execute.py \
  --payload-file MOA/templates/VIP-增加经验值.json \
  --vip-user-id <userId> \
  --vip-level 6

# ② 重置头像框上传冷却（Admin）
python3 Admin/admin_execute.py \
  --reset-custom-prop-cooldown \
  --custom-prop-remote-id <userId> \
  --custom-prop-type HEADER_FRAME
```

<a id="setup_custom_vehicle"></a>

### 定制座驾-开通流程

- **功能**：定制座驾需 VIP8。按顺序执行：① MOA 升到 VIP8；② Admin 重置座驾上传冷却（resetCoolDown）
- **提示词**：
  - `用户 <userId> 需要定制座驾`
  - `<userId> 需要定制座驾，先升级 VIP8 再重置上传`
  - `100465989 需要定制座驾`
- **命令**：

```bash
# ① 升到 VIP8（MOA）
python3 MOA/moa_execute.py \
  --payload-file MOA/templates/VIP-增加经验值.json \
  --vip-user-id <userId> \
  --vip-level 8

# ② 重置座驾上传冷却（Admin）
python3 Admin/admin_execute.py \
  --reset-custom-vehicle-cooldown \
  --custom-vehicle-remote-id <userId>
```

<a id="admin-cat-6"></a>

## 6) 定制道具（yaahlan-admin）

<a id="reset_custom_prop_cooldown"></a>

### 定制头像框-重置上传冷却

- **功能**：重置用户定制道具上传冷却（resetCoolDownProp；params=remoteId、propType）。头像框默认 propType=HEADER_FRAME；需 VIP6 才可定制头像框；审核中或当前可定制时可能不支持重置
- **提示词**：
  - `重置用户 <userId> 自定义头像框上传时间`
  - `重置 <userId> 定制头像框 cooldown`
  - `清除 <userId> HEADER_FRAME 上传冷却`
  - `用户 <userId> 需要定制头像框，先升 VIP6 再重置头像框上传冷却`
  - `<userId> 需要定制头像框`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --reset-custom-prop-cooldown \
  --custom-prop-remote-id <userId> \
  --custom-prop-type HEADER_FRAME
```

<a id="admin-cat-7"></a>

## 7) 客服（melon-gateway / cms/customerservice）

<a id="change_cs_taking_order"></a>

### 客服-修改接单状态

- **功能**：修改客服接单状态（changeTakingOrder；params=userId、takingOrder）。0 不接单；1 接单
- **提示词**：
  - `修改客服 <userId> 接单状态`
  - `<userId> 停止接单`
  - `客服 <userId> 开始接单`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --change-cs-taking-order \
  --cs-user-id <userId> \
  --cs-taking-order 0
```

<a id="save_cs_data"></a>

### 客服-新增/编辑账号

- **功能**：新增或编辑客服账号（saveCsData）。默认创建 VIP客服（roleList=1）、启用、接单。身份：1 VIP；2 游戏；3 语音房；4 Admin；5 公会通知审核；6 公会运营。optType：1 创建；2 编辑
- **提示词**：
  - `给 <userId> 添加 VIP 客服`
  - `新增客服账号 <userId>`
  - `<userId> 添加 VIP 客服`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --save-cs-data \
  --cs-user-id <userId> \
  --cs-role-list 1 \
  --cs-enable 1 \
  --cs-taking-order 1 \
  --cs-opt-type 1
```

<a id="query_cs_data"></a>

### 客服-查询账号列表

- **功能**：分页查询客服账号列表（queryCsData；可按 userId、role、enable、area 筛选；含昵称、角色、接单状态、今日单量等）
- **提示词**：
  - `查询客服账号列表`
  - `查 userId <userId> 是否是客服`
  - `列出全部客服账号`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-cs-data --cs-page-index 1 --cs-page-size 20
```

<a id="admin-cat-8"></a>

## 8) 家族（melon-gateway）

<a id="add_family_member"></a>

### 家族-增加成员

- **功能**：把用户加入指定家族（addFamilyMember；params=familyId, userId）。若用户已在该家族，msg 会提示添加失败
- **提示词**：
  - `在家族 <familyId> 增加用户 <userId>`
  - `把 <userId> 加入家族 <familyId>`
  - `家族 <familyId> 添加成员 <userId>`
- **命令**：

```bash
python3 Admin/admin_execute.py --add-family-member --family-id <familyId> --family-user-id <userId>
```

<a id="query_family"></a>

### 家族-查询信息

- **功能**：按家族 ID 或名称查询家族信息（queryFamilyByIdAndName；含头像、等级、家族长、人数、活跃人数、加入/退出总数）。成员 userId 列表可配合 MOA `--family-query-members` 或 `--family-detail`
- **提示词**：
  - `查询家族 <familyId> 信息`
  - `查家族 <familyName> 详情`
  - `家族 <familyId> 当前人数`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-family --family-id <familyId>
```

<a id="list_all_families"></a>

### 家族-查询全部列表

- **功能**：分页查询全部家族列表（getAllFamilyList；含家族长 familyOwnerId、等级、人数等）。无需 familyId/familyName 筛选
- **提示词**：
  - `查询全部家族列表`
  - `列出所有家族`
  - `家族列表 offset <offset> limit <limit>`
- **命令**：

```bash
python3 Admin/admin_execute.py --list-all-families --family-offset <offset> --family-limit <limit>
```

<a id="admin-cat-9"></a>

## 9) 版本（melon-gateway / backend/pangu）

<a id="query_app_store_review_version"></a>

### 版本-查询 App Store 审核版本

- **功能**：查询 iOS App Store 审核版本与 Android versionCode（getAppStoreReviewVersion；params=appName，默认 yaahlan）
- **提示词**：
  - `查询 iOS 审核版本`
  - `当前 App Store 审核版本是多少`
  - `yaahlan 审核版本`
  - `getAppStoreReviewVersion`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-app-store-review-version
```

<a id="update_app_store_review_version"></a>

### 版本-设置 App Store 审核版本

- **功能**：设置 iOS App Store 审核版本与 Android versionCode（updateAppStoreReviewVersion；params=appName、iosVersion、androidVersionCode）
- **提示词**：
  - `设置 iOS 审核版本为 <iosVersion>`
  - `把 App Store 审核版本改成 <iosVersion>`
  - `updateAppStoreReviewVersion <iosVersion>`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --update-app-store-review-version \
  --ios-version <iosVersion> \
  --android-version-code <androidVersionCode>
```

<a id="admin-cat-10"></a>

## 10) 用户（mdp-nova / userAdmin + MOA）

<a id="batch_mutual_friends_from_user_list"></a>

### 用户-批量互关结好友

- **功能**：从 ops-admin 用户列表选取 userId，通过 addUserRelation 双向互关为目标用户增加好友。须 Admin/.env.local 配置 MDP 鉴权；MOA 走 Stage Redis 直连
- **提示词**：
  - `给用户 <userId> 从列表互关增加 <count> 个好友`
  - `从用户列表给 <userId> 批量结好友 <count> 个`
  - `用户 <userId> 批量互关结好友`
- **命令**：

```bash
python3 Admin/scripts/batch_mutual_friends_from_user_list.py \
  --target-user-id <userId> \
  --count <count>
```

<a id="admin-cat-11"></a>

## 11) 用户（mdp-nova / userAdmin）

<a id="query_user_profile_list"></a>

### 用户-查询列表

- **功能**：查询测试环境用户列表（userAdmin/queryUserProfileList；同 ops-admin 用户列表页；可按 userId/昵称/手机号/mmuidv3/设备ID 等筛选）
- **提示词**：
  - `查询用户列表`
  - `按 userId <userId> 查用户列表`
  - `按手机号 <phone> 查用户列表`
  - `按 mmuidv3 <mmuidv3> 查用户列表`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --query-user-list \
  --user-list-page-no 1 \
  --user-list-page-size 20
```

<a id="cancel_user"></a>

### 用户-注销账号

- **功能**：注销用户账号（userAdmin/cancelUser；不可逆）。须 --cancel-user-confirm 二次确认；13311111111~13311111130 受保护不可注销
- **提示词**：
  - `注销用户 <userId> 账号`
  - `注销 userId <userId>`
  - `后台 cancelUser <userId>`
- **命令**：

```bash
python3 Admin/admin_execute.py \
  --cancel-user \
  --cancel-user-id <userId> \
  --cancel-user-confirm
```

<a id="admin-cat-12"></a>

## 12) 用户（yaahlan-admin）

<a id="query_user_feed_list"></a>

### 用户-查询动态列表

- **功能**：按 userId 分页查询用户全部动态（queryUserFeedList；feedId、发布时间、点赞/评论/打赏、图文视频数量等）
- **提示词**：
  - `查询用户 <userId> 的动态列表`
  - `查 <userId> 发了哪些动态`
  - `用户 <userId> 动态 feed 列表`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-user-feed-list --query-user-id <userId>
```

<a id="query_user_history_device_list"></a>

### 用户-查询历史登录设备

- **功能**：按 userId 查询账号历史登录设备列表（deviceHistory/queryUserHistoryDeviceList；mmuidv3、IP、UA、最近登录时间等）
- **提示词**：
  - `查询用户 <userId> 历史登录设备`
  - `查 <userId> 登录过的设备`
  - `用户 <userId> 设备 mmuidv3 列表`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-user-history-devices --history-device-user-id <userId>
```

<a id="query_history_user_list_by_device_id"></a>

### 用户-查询设备历史登录账号

- **功能**：按 mmuidv3 查询该设备历史登录过的账号列表（deviceHistory/queryHistoryUserListByDeviceId；userId、昵称、是否主播、公会等）
- **提示词**：
  - `查询设备 <mmuidv3> 登录过的账号`
  - `查 mmuidv3 <mmuidv3> 历史用户列表`
  - `这台设备登录过哪些 userId`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-device-history-users --history-device-mmuidv3 <mmuidv3>
```

<a id="query_user_detail"></a>

### 用户-查询详情

- **功能**：按 userId 查询用户全量详情（queryUserDetail；昵称、手机、等级、钻石、房间、公会、设备 mmuid/mmuidv3、业务状态等）
- **提示词**：
  - `查询用户 <userId> 详情`
  - `查 userId <userId> 的资料`
  - `用户 <userId> 房间 ID 是多少`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-user-id <userId>
```

<a id="query_device_current_account"></a>

### 设备-查询当前登录账号

- **功能**：按测试机名称或 mmuidv3 查当前登录 userId：test_devices.json 取 mmuidv3 → queryHistoryUserListByDeviceId 取最近 updateTime → queryUserDetail 校验 loginDevice（详见 .cursor/skills/device-current-account/SKILL.md）
- **提示词**：
  - `红米12当前登录的账号是什么`
  - `这台设备登录的是哪个账号`
  - `后台查询设备当前登录用户`
  - `查测试机当前 userId`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-device-history-users --history-device-mmuidv3 <mmuidv3> && python3 Admin/admin_execute.py --query-user-id <userId>
```

<a id="admin-cat-13"></a>

## 13) 礼物（mdp-nova / giftAdmin）

<a id="query_gift_list"></a>

### 礼物-查询列表

- **功能**：查询 Yaahlan 全量礼物配置（giftAdmin/queryGiftList）。已上线背包礼物：giftType=1、giftStatus=1（giftSubType 留空）；返回 baseId、名称、价格、状态
- **提示词**：
  - `查询礼物列表`
  - `查已上线背包礼物列表`
  - `查礼物 baseId <baseId> 信息`
  - `礼物 <productName> 的 baseId 是多少`
  - `后台礼物配置 queryGiftList`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-gift-list --gift-type 1 --gift-status 1 --gift-page-no 1 --gift-page-size 20
```

<a id="admin-cat-14"></a>

## 14) 道具（mdp-nova / propAdmin）

<a id="query_prop_info"></a>

### 道具-查询配置列表

- **功能**：查询 Yaahlan 全量装扮/道具后台配置（propAdmin/queryPropInfo；可按 propId、propName、propTypeCode 筛选；与 MOA queryOwnPropList 互补：本接口查配置，MOA 查用户拥有）
- **提示词**：
  - `查询道具配置列表`
  - `查道具 propId <propId> 信息`
  - `道具 <propName> 的 propId 是多少`
  - `头框道具配置有哪些`
  - `后台道具 queryPropInfo`
- **命令**：

```bash
python3 Admin/admin_execute.py --query-prop-list --prop-page-no 1 --prop-page-size 20
```
