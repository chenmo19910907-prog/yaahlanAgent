# 礼物面板 × Tunnel 抓包

打开礼物面板（点底部**橙色礼物盒**，非快捷礼物）后，客户端会拉取礼物列表。

## 关键接口

| 接口 | 说明 |
|------|------|
| `POST .../component/giftPanel/getGiftTabListV3` | **各 Tab 与礼物列表**（name / id / bid / price） |
| `POST .../rank/room/gift/getTotalCustomGiftRankList` | **自定义礼物周榜**（`data.list[]` 按 `value` 排名；`cycle=1` 本周、`cycle=2` 上周） |
| `POST .../component/giftPanel/getGiftPanel` | Banner、面板配置 |
| `POST .../component/giftPanel/sendCheckInfo` | 送礼前校验（**cpUserId**、财富/贵族等级等） |
| `POST .../component/giftPanel/propPackageList` | 背包道具 |
| `POST .../component/giftPanel/getGiftTabListV3` → **`背包` Tab** | **背包礼物**（`is_package=1`；读 `package.remain` / `expire` / `label`） |
| `POST .../v2/gift/send` | **实际送礼**（Tunnel keyword 用 `gift/send` 或 `v2/gift/send`，非 `sendGift`） |

> **背包礼物 vs 背包道具**：`propPackageList` 仅道具；**礼物**在 `getGiftTabListV3` 的 `tab_name=背包` 且 `is_package=1`。下发 MOA `addPackageGift` 后验收 `package.remain`（`bid` = baseProductId，面板 `id` 可能不同）。

## 背包礼物验收（MOA 下发后）

```bash
# 用户已在 App 打开礼物面板后
python3 adb/adb_execute.py gift panel backpack \
  --momoid <userId> --bid 2005001494 --expect-remain 10 --since 600

# 或统一脚本（工作流第二步同款）
python3 MOA/scripts/package_gift_backpack_tunnel_verify.py \
  --user-id <userId> --bid 2005001494 --expect-remain 10 --since 600
```

响应示例（`getGiftTabListV3` 背包 Tab）：

```json
{
  "name": "Chocolate",
  "id": "2005001496",
  "bid": 2005001494,
  "package": { "remain": 10, "expire": 1783180740, "label": "07.04 23:59失效" }
}
```

工作流：

```bash
# 仅 Tunnel 验收（须先开礼物面板）
python3 workflow/workflow_execute.py run package-gift-backpack-verify \
  --user-id 100007541 --base-product-id 2005001494 --expect-remain 10

# MOA 下发 + Tunnel 验收（两步之间须手动打开礼物面板）
python3 workflow/workflow_execute.py run package-gift-dispatch-verify \
  --user-id 100007541 --num 10
```

## UI 与数据对应

- **左右滑** Tab 栏 ↔ `getGiftTabListV3.gift_list[].tab_name`（Gift、Lucky、privilege、**Customize**、Relationship、props、Country、背包…）
- **上下滑** 礼物格 ↔ 同一 Tab 下 `list[]` 按 4 列网格展示；列表下标 `index` 可换算行/列与需上滑次数
- **Customize Tab** ↔ `tab_name=Customize` 的 `list[]`；**数组顺序即面板展示排序**（与自定义礼物周榜排名无关）

### getGiftTabListV3 缓存命中

同一轮打开礼物面板可能连续请求两次：

| 次序 | 特征 | 能否解析排序 |
|------|------|--------------|
| 第一次 | `response.data.gift_list` 存在 | ✅ |
| 第二次 | `request.giftListHash` = `response.gift_list_hash`，**无 `gift_list`** | ❌ |

Agent / 脚本取**含 `gift_list` 的最近一条**，勿只用最新一条。

## CLI

```bash
# 打开礼物面板后：列出各 Tab 礼物数与价位
python3 adb/adb_execute.py gift panel analyze --account familyLeader --since 120

# 查 99 钻礼物（含 Tab、index、上下滑提示）
python3 adb/adb_execute.py gift panel find --account familyLeader --price 99 --tab Gift

# 按 userId + 线上 overseas 抓包解析（提示词须含「线上环境」时用 online tunnel 拉包，再 analyze）
python3 adb/adb_execute.py gift panel analyze --momoid <userId> --since 7200 --g-env overseas
```

### 线上环境：Customize 排序 / 自定义礼物榜单

```bash
# 1) Customize Tab 礼物面板排序（用户须刚在 App 打开礼物面板）
python3 online/online_execute.py tunnel --momoid <userId> --keyword getGiftTabListV3 --since 7200 --output json
# → 取含 gift_list 的响应，筛选 tab_name=Customize 的 list[]

# 2) 自定义礼物周榜（用户须在 App 切过本周/上周 Tab）
python3 online/online_execute.py tunnel --momoid <userId> --keyword getTotalCustomGiftRankList --since 7200 --output json
# → request.cycle=1 本周，cycle=2 上周；data.list[] 字段：giftId、giftName、value、ownerUid、ownerNickname
```

导出桌面示例（解析后写 CSV/JSON）：

| 文件命名建议 | 内容 |
|--------------|------|
| `customize_gift_sort_<userId>_online.json` | Customize Tab `list[]` 全量排序 |
| `custom_gift_rank_<userId>_online.json` | 本周榜 `cycle=1` |
| `custom_gift_rank_last_week_<userId>_online.json` | 上周榜 `cycle=2` |

## 示例（Gift Tab 99 钻）

| index | name | id | price |
|-------|------|-----|-------|
| 18 | Trophy | 2005056028 | 99 |
| 38 | Huggy Bear | 2005014925 | 99 |
| 96 | little heart | 2005001770 | 99 |
| 98 | roses | 2005001776 | 99 |

送礼流程：Tunnel `find` 定 Tab 与 index → ADB 切 Tab + 上下滑 → 点选 → Send。

## 送礼验收（必读 response.em）

点 **Send** 后**必须抓包读 `v2/gift/send` 的响应体**，Toast/公屏不可靠：

| 字段 | 含义 |
|------|------|
| `response.ec` | **200** = 业务成功；非 200 = 失败 |
| `response.em` | **失败原因文案**（优先读此字段） |
| `response.data` | 补充信息（余额不足、CP 关系、等级限制等） |

```bash
# 点 Send 后等待并校验（expect-ec 200 时，抓到但 ec≠200 会返回 failureReason）
python3 adb/adb_execute.py tunnel wait --account guildLeader --keyword gift/send --since 30 --wait 25 --expect-ec 200

# 或读取最近一条 gift/send 结果（含 failureReason）
python3 adb/adb_execute.py tunnel last --account guildLeader --keyword gift/send --since 120
```

**注意**：

- 仅「未抓到 `gift/send`」≠ 业务成功，可能是 Send 未点中、未选收礼人、或客户端拦截未发请求。
- 已抓到但 `ec≠200`：`tunnelVerify.businessFailure=true`，读 `matches[0].failureReason`。
- **Room All** 送礼：点 Send 后会出现二次确认（Cost / Confirm），须点 **Confirm** 才会发出 `gift/send`。

## CP 礼物（只能送给 CP）

**业务规则**：名称含 **CP** 的礼物（如 **CP Hot Air Balloon** 100 钻）**只能送给 CP（情侣/搭档）**，不能送给普通麦位用户或 Room All。

| 抓包信号 | 含义 |
|----------|------|
| `sendCheckInfo.data.cpUserId` | 当前账号的 CP 对象 userId；**`null` = 无 CP，不可送 CP 礼物** |
| 客户端 Toast | 常见提示「CP 礼物不可以赠送 / 只能送给 CP」 |
| 无 `gift/send` | 客户端在校验阶段拦截，请求未发出 |

**自动化前预检**（打开礼物面板后）：

```bash
# 读 sendCheckInfo（gift 面板打开后会出现）
python3 -c "
from adb.adb.popup_analyze import fetch_recent_tunnel_items
items,_=fetch_recent_tunnel_items(momoid='100465989', since_seconds=120)
for x in sorted(items, key=lambda i: str(i.get('time','')), reverse=True):
    if 'sendCheckInfo' in str(x.get('url','')):
        d=(x.get('response') or {}).get('data') or {}
        print('cpUserId', d.get('cpUserId'), 'buddyUserIdSet', d.get('buddyUserIdSet'))
        break
"
```

`cpUserId` 为 `null` 时，**不要测 CP 礼物**；改测非 CP 礼物（如 Trophy 99 钻），或换有 CP 关系的账号。

若仍尝试送出且服务端有响应，读 `gift/send` 的 `response.em` 获取失败原因。
