## 已录入 Gift 清单（自动生成）

> 本文件由 `Gift/scripts/generate_index.py` 根据 `Gift/config/registry.json` 自动生成，请勿手动编辑。

### 目录

- [1) Stage HTTP 送礼（/v2/gift/send）](#gift-cat-1)
  - [Stage送礼-全房间](#stage_gift_room_all)
  - [Stage送礼-房间内](#stage_gift_chatroom)
  - [Stage送礼-探测](#stage_gift_probe)
  - [Stage送礼-私聊](#stage_gift_private)
  - [Stage送礼-群组](#stage_gift_group)
  - [Stage送礼-预演](#stage_gift_dry_run)

### 使用说明

- **环境**：仅 Stage（alpha/stage），禁止用于线上
- **鉴权**：复制 `Gift/.env.example` 为 `Gift/.env.local`，填入 `CMDB_TOKEN`
- **与 MOA 背包区别**：本模块直连 `/v2/gift/send`；MOA `addPackageGift` 仅下发背包，`sendMiddlePackageGift` 通常不触发真实送礼
- **UI 验收**：客户端流程仍须 ADB 礼物面板 + Tunnel `gift/send ec=200`
- **输出**：stdout 为 JSON；`response.ec != 0` 时读 `error` / `response.em`

<a id="gift-cat-1"></a>

## 1) Stage HTTP 送礼（/v2/gift/send）

<a id="stage_gift_room_all"></a>

### Stage送礼-全房间

- **功能**：测试环境语音房全房送礼（自动调 getSendRoomAllSnap 获取 snapId，无需 receivers）
- **提示词**：
  - `Stage 用户 <sender> 在房间 <roomId> 全房送礼物 <giftId>`
  - `测试环境全房间送礼 sender=<sender> room=<roomId> giftId=<giftId>`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --scene chatroom \
  --sender <sender> \
  --gift-id <giftId> \
  --scene-id <roomId> \
  --num <num> \
  --send-room-all
```

<a id="stage_gift_chatroom"></a>

### Stage送礼-房间内

- **功能**：测试环境直连 yaahlan-web POST /v2/gift/send，语音房内给指定用户送礼（CMDB 查实例 + MOA 查礼物与用户设备）
- **提示词**：
  - `Stage 用户 <sender> 在房间 <roomId> 给 <receivers> 送礼物 <giftId>`
  - `测试环境房间内送礼 sender=<sender> room=<roomId> receivers=<receivers> giftId=<giftId>`
  - `stage 送礼 chatroom <sender> <roomId> <receivers> <giftId>`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --scene chatroom \
  --sender <sender> \
  --receivers <receivers> \
  --gift-id <giftId> \
  --scene-id <roomId> \
  --num <num>
```

<a id="stage_gift_probe"></a>

### Stage送礼-探测

- **功能**：只探测 CMDB/MOA（实例 IP、礼物元数据、用户设备），不 POST 送礼
- **提示词**：
  - `Stage 探测送礼参数 sender=<sender> giftId=<giftId>`
  - `测试环境送礼 probe`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --probe \
  --scene <chatroom|private|group> \
  --sender <sender> \
  --receivers <receivers> \
  --gift-id <giftId> \
  [--scene-id <roomId|groupId>]
```

<a id="stage_gift_private"></a>

### Stage送礼-私聊

- **功能**：测试环境 IM 私聊送礼（scene=private，无需 scene-id）
- **提示词**：
  - `Stage 用户 <sender> 私聊给 <receiver> 送礼物 <giftId>`
  - `测试环境私聊送礼 sender=<sender> receiver=<receiver> giftId=<giftId>`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --scene private \
  --sender <sender> \
  --receivers <receiver> \
  --gift-id <giftId> \
  --num <num>
```

<a id="stage_gift_group"></a>

### Stage送礼-群组

- **功能**：测试环境群组内送礼（scene=group，scene-id 为 groupId）
- **提示词**：
  - `Stage 用户 <sender> 在群组 <groupId> 给 <receivers> 送礼物 <giftId>`
  - `测试环境群组送礼 sender=<sender> group=<groupId> receivers=<receivers> giftId=<giftId>`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --scene group \
  --sender <sender> \
  --receivers <receivers> \
  --gift-id <giftId> \
  --scene-id <groupId> \
  --num <num>
```

<a id="stage_gift_dry_run"></a>

### Stage送礼-预演

- **功能**：组装完整 /v2/gift/send payload 但不 POST（dry-run）
- **提示词**：
  - `Stage dry-run 送礼 sender=<sender> giftId=<giftId>`
  - `测试环境送礼 dry-run`
- **命令**：

```bash
python3 Gift/gift_execute.py \
  --dry-run \
  --scene <chatroom|private|group> \
  --sender <sender> \
  --receivers <receivers> \
  --gift-id <giftId> \
  [--scene-id <roomId|groupId>]
```
