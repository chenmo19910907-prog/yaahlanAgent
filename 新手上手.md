# 新手上手引导

> 适用分支：`yaahlan`。`git pull` 只会同步代码，**不会**同步各人的登录 Cookie / Token。按本文完成本地配置后，即可使用 MOA、Admin、Risk、Tunnel、Report、ADB 真机与 Cursor 用例能力。

## 你需要准备什么

| 项 | 要求 |
|----|------|
| Python | **3.8+**（`python3 --version`） |
| 网络 | 能访问 `mse.wemomo.com`、测试后台等内网域名（通常需公司 VPN） |
| 浏览器 | 已登录 MSE MOA、Yaahlan Admin、钉钉文档（按需） |
| Cursor | 用于 Agent / MCP 生成用例（可选） |
| Android 真机 | 做 ADB 自动化时需 USB 调试或 `adb connect`（可选） |
| adb | Android platform-tools（可选，与真机配套） |

**配置耗时参考**：MOA + Admin 约 **10 分钟**；Cursor MCP 首次约 **15 分钟**（含 venv）；Report 虚拟环境约 **3 分钟**；ADB 前置（装 adb + 连机）约 **5 分钟**。

---

## 第一步：拉代码

```bash
git clone git@git.wemomo.com:soulchill-qa/auto-generate-testcase.git
cd auto-generate-testcase
git checkout yaahlan
git pull origin yaahlan
```

> 切换其他分支（如 `hanmin`）前请先 **stash / 提交** 本地修改，避免与 `.cursor/mcp.json`、各 `.env.local` 冲突。

---

## 第二步：配置文件一览（必看）

以下 `.env.local` **不在 Git 里**，每台电脑需自己从 `.env.example` 复制并填写。

| 用途 | 复制命令 | 本地配置文件 | 是否常用 |
|------|----------|--------------|----------|
| **MOA**（VIP/房间/钻石等） | `cp MOA/.env.example MOA/.env.local` | `MOA/.env.local` | **必配** |
| **Admin**（查用户详情/房间 ID） | `cp Admin/.env.example Admin/.env.local` | `Admin/.env.local` | **推荐** |
| **Tunnel**（HTTP 抓包查询） | 一般无需单独建 | 复用 `MOA/.env.local` 的 Cookie | 推荐 |
| **Risk**（风控加白/解除） | **无需** `.env.local` | 默认读 `Risk/config.json` | 按需 |
| **钉钉 MCP / Report** | 编辑已有文件 | `.cursor/mcp.json` 内 `env` | 用 Cursor 生成用例或 Report 时 |
| **DingTalk**（目录列举 / kb 同步） | 一般无需单独建 | `DingTalk/config/kb.json` + 同上 Cookie | 维护 testcase-kb 时 |

**已在仓库中、一般不用改**：`MOA/config/thresholds.json`、`MOA/config/registry.json`、`MOA/templates/*.json`、`Admin/config.json`、`Risk/config.json`、`DingTalk/config/kb.json`、`adb/录制脚本/索引.json`。

---

## 第三步：配置 MOA（约 5 分钟）

### 3.1 创建配置文件

```bash
# 可选：部分能力依赖 redis/PyYAML（见 MOA/requirements.txt）
python3 -m venv MOA/.venv
MOA/.venv/bin/pip install -r MOA/requirements.txt

cp MOA/.env.example MOA/.env.local
```

未建 `MOA/.venv` 时，多数模板仍可用 `.env.local` 直接跑；团队 Redis 共享 Cookie 见 [MOA/README.md](MOA/README.md)（`moa.yaml`，维护者向）。

### 3.2 获取 Cookie

1. 浏览器打开 [MSE 管理平台](https://mse.wemomo.com) 并登录  
2. 打开任意 MOA 测试请求（F12 → 网络）  
3. 复制请求头里完整的 **`Cookie:`** 一行（含 `JSESSIONID`、`tunnel_login_session`、`auth_cookie` 等）

### 3.3 写入 `MOA/.env.local`

```bash
MOA_ENTRY_URL=https://mse.wemomo.com/apirest/httpproxy/moa/test
MOA_COOKIE=这里粘贴完整 Cookie
MOA_ORIGIN=https://mse.wemomo.com
MOA_REFERER=https://mse.wemomo.com/
MOA_USER_AGENT=Mozilla/5.0
MOA_REQUEST_SOURCE=moaProxy
```

### 3.4 验证 MOA

```bash
python3 MOA/moa_execute.py \
  --payload-file "MOA/templates/VIP-增加经验值.json" \
  --vip-user-id 100465989 \
  --vip-query-current
```

成功时应输出 JSON，含 `vipLevel`、`currentVipExp` 等字段。

### 3.5 自然语言 / 命令速查

- 能力清单与可复制命令：[MOA/使用方法.md](MOA/使用方法.md)  
- 技术说明与抓包约定：[MOA/README.md](MOA/README.md)  
- 对 Cursor 说：「用户 `<userId>` 升级到 VIP5」→ Agent 会按清单调用脚本  

示例：

```bash
# 永久升到 VIP5
python3 MOA/moa_execute.py \
  --payload-file "MOA/templates/VIP-增加经验值.json" \
  --vip-user-id <userId> \
  --vip-level 5

# 下发 1 天 VIP5 体验（秒）
python3 MOA/moa_execute.py \
  --payload-file "MOA/templates/VIP-下发体验卡.json" \
  --vip-try-user-id <userId> \
  --vip-try-level 5 \
  --vip-try-duration-seconds 86400

# 设置房间等级
python3 MOA/moa_execute.py \
  --payload-file "MOA/templates/房间-设置等级.json" \
  --room-set-level-room-id <roomId> \
  --room-set-level <level>

# 可选：批量自测全部 MOA 模板
python3 MOA/scripts/test_all.py
```

---

## 第四步：配置 Admin（查手机号 → userId → 房间）

### 4.1 创建配置文件

```bash
cp Admin/.env.example Admin/.env.local
```

### 4.2 获取 Token

1. 打开 [Yaahlan 测试后台](https://yaahlan-admin-alpha.wemomo.com) 并登录  
2. F12 → 任意接口请求 → 请求头复制：  
   - `sso-token` → `ADMIN_SSO_TOKEN`  
   - `yaahlan-jwt` → `ADMIN_YAAHLAN_JWT`  

### 4.3 验证 Admin

```bash
python3 Admin/admin_execute.py --query-user-id 100465989
```

应返回昵称、手机、**roomId**、VIP 等级等摘要 JSON。

### 4.4 手机号查 userId（MOA）

```bash
python3 MOA/moa_execute.py \
  --payload-file "MOA/templates/用户-按手机号查userId.json" \
  --query-user-by-phone 13311111111 \
  --phone-output summary
```

典型链路：**手机号 → MOA 得 userId → Admin 得 roomId / 资料**。

更多后台能力（公会、定制礼物、客服等）见 [Admin/使用方法.md](Admin/使用方法.md)。

---

## 第五步：Tunnel 抓包（推荐，可复用 MOA Cookie）

已配置 `MOA/.env.local` 时，一般**无需**再建 `Tunnel/.env.local`（MOA Cookie 含 `tunnel_login_session`）。若 MOA 与 Tunnel 登录态分离，可 `cp Tunnel/.env.example Tunnel/.env.local` 单独填 `TUNNEL_COOKIE`。

```bash
# 查某 userId 最近 1 小时 HTTP 抓包
python3 Tunnel/tunnel_execute.py --momoid 100486375 --since 3600

# 按 URL 关键字过滤（如送礼接口）
python3 Tunnel/tunnel_execute.py --momoid 100486375 --keyword gift --since 7200
```

成功时应返回该 userId 的 HTTP 请求列表 JSON（含 URL、时间等），而非 HTML 登录页。

说明与 API 参数见 [Tunnel/README.md](Tunnel/README.md)、[Tunnel/使用方法.md](Tunnel/使用方法.md)。ADB 真机操作后验收接口时，常与 Tunnel 联动（见 [adb/使用方法.md](adb/使用方法.md) 第 4 节）。

---

## 第六步：Risk 风控（可选，无需 `.env.local`）

开放接口默认使用 `Risk/config.json` 中的 `token`（`sec_risk_IHAH`）与 `base_url`，**clone 后可直接调用**。设备解除读 `testcase-kb/test_devices.json`（Android/鸿蒙取 **mmuidv3**，iOS 取 **mmuid**）。仅当需覆盖 token / 域名时再 `cp Risk/.env.example Risk/.env.local`。

详见 [Risk/README.md](Risk/README.md)、[Risk/使用方法.md](Risk/使用方法.md)。

```bash
python3 Risk/risk_execute.py --help
python3 Risk/risk_execute.py --list-test-devices

# 示例：解除测试机设备风控（设备名见 test_devices.json）
python3 Risk/risk_execute.py --release-test-device --device-name "GalaxyA80" --reason 测试
```

---

## 第七步：Cursor 用例生成（可选）

### 7.1 首次安装 MCP 依赖（新机器必做）

`.cursor/mcp.json` 指向各 Skill 目录下的 **venv**，clone 后需本地创建（venv 不入库）：

```bash
# 钉钉文档 MCP（建议 Python 3.10+，本机可用 python3.13）
cd .cursor/skills/dingtalk-doc-read/mcp_dingtalk_doc
python3.13 -m venv venv   # 无 3.13 时用 python3 -m venv venv
venv/bin/pip install -e .

# 钉钉 Excel 读写 MCP（read / write 共用同一 venv）
cd ../../testcase-to-excel/mcp_dingtalk_excel
python3.13 -m venv venv
venv/bin/pip install -e .
```

`.cursor/mcp.json` 中 `command` 须指向上述 venv 里的解释器（如 `venv/bin/python3.13`）。若 venv 损坏（解释器 symlink 失效），删除 `venv` 目录后按上式重建。

安装完成后 **Reload Window**（Cmd+Shift+P → Developer: Reload Window），再在 Cursor Settings → MCP 确认三个服务为绿色。

### 7.2 配置钉钉凭证

编辑 `.cursor/mcp.json` 中各服务器 `env`：

| 服务器 | 关键环境变量 | 获取方式 |
|--------|----------------|----------|
| `dingtalk-doc` | `DINGTALK_COOKIE` | 浏览器打开 [钉钉文档](https://alidocs.dingtalk.com) 并登录 → F12 → Network → 刷新 → 复制请求头 **Cookie** |
| `dingtalk-excel-read` / `write` | `DINGTALK_AEGIS_KEY`、`DINGTALK_AEGIS_SECRET`、`DINGTALK_WORKID` | [Aegis 我的应用](https://aegis.immomo.com/v3/app/my) 创建应用得 Key/Secret；`WORKID` 为工号 |

**Cookie / Aegis 失效时**：重新抓包更新 `env` → Reload Window。文档 MCP 也可对 Agent 提供新 Cookie，调用 `refresh_cookie` 后重试 `parse_document`。

**安全**：个人 Cookie/Token **勿提交 Git**；若 `.cursor/mcp.json` 含个人凭证，提交前确认未泄露或改用本地 override。

### 7.3 推荐流程

1. 提供钉钉需求文档 URL  
2. Agent 使用 `prd-review`、`dingtalk-doc-read` 解析需求  
3. 结合 `rules/testcase_generation_rules.md`、`templates/`、`documents/`、`testcase-kb/` 补全维度  
4. 用例写入 `temporary_testcase/`（Markdown 表格）  
5. 使用 `testcase-to-excel` 同步到钉钉 Excel  

**分支说明**：

- **营收活动 / 通用模块**：主流程见 [SKILL.md](SKILL.md)  
- **动态（Moments）**：见 [moments/README.md](moments/README.md)（与 SKILL 同构）  
- **发版 / 版本回归**：须先读 [rules/version_testcase_generation_rules.md](rules/version_testcase_generation_rules.md)，并在 `documents/` 找对应模块文档  

**常用 Skill**（见 `.cursor/skills/`）：

| Skill | 用途 |
|-------|------|
| `dingtalk-doc-read` | 读钉钉需求文档 |
| `prd-review` | 生成前 PRD 理解与边界梳理 |
| `testcase-generator` / `testpoints-to-testcases` | 用例生成与测试点扩写 |
| `testcase-to-excel` | 写入钉钉 Excel |
| `testcase-handwritten-excel-style` / `user-testcase-style-shortphrase` | 对齐手写表/短语风格 |
| `adb-page-learn` / `adb-tunnel-verify` / `tunnel-read` | 真机探索与抓包验收 |

### 7.4 写入钉钉 Excel

Agent 使用 `testcase-to-excel` 时约定：

| 列 | 说明 |
|----|------|
| 编号 | 用例 ID，如 `001` |
| 功能模块 | 模块名（可与用例标题合并） |
| 测试步骤 | 1. 2. 3. 分行可执行步骤 |
| 预期结果 | 界面反馈、状态变化、接口响应 |

- 从 `temporary_testcase/*.md` 读取后映射为上表四列  
- **超过约 50 行须分批**写入，递增 `startRow`  
- 失败提示 `InvalidAuthentication` → 更新 Aegis 后重试  

### 7.5 知识库（生成前按需阅读）

| 目录 | 用途 |
|------|------|
| `templates/` | 与钉钉表对齐的模块用例骨架（榜单、抽奖等） |
| `testcase-kb/` | 版本用例汇总的产品规则 / 验收要点（设计 / 回归参考） |
| `verified-kb/` | 真机已跑通的功能验收要点（与 `testcase-kb` 互补，ADB 探索起点） |
| `bug-kb/` | 历史缺陷归档 |
| `online-kb/` | 线上问题子集 |
| `documents/` | 业务模块说明（如 `gift.md`、动态 `moments/`） |

```bash
python3 scripts/suggest_kb_for_module.py 礼物   # 生成前查看应读知识库
python3 DingTalk/kb_sync_execute.py --list-only  # 列举钉钉用例目录内版本 Excel
python3 DingTalk/kb_sync_execute.py            # 维护者：钉钉目录 → testcase-kb
python3 scripts/sync_all_kb.py                # 维护者：bug-kb / online-kb（任务表 xlsx）
python3 scripts/export_testcases_to_desktop.py # 导出 temporary_testcase 副本（按需）
```

### 7.6 工具链自检（推荐）

```bash
python3 scripts/doctor.py
python3 scripts/doctor.py --run-moa-probe       # 额外探活 MOA
python3 scripts/doctor.py --check-testcases   # 校验 temporary_testcase 下用例
python3 scripts/check_testcase_md.py
```

---

## 第八步：Report 测试报告（可选）

从版本用例 xlsx 或钉钉 Excel URL 生成内网 / 外网测试总结 HTML。

### 8.1 准备 Python 依赖

```bash
python3 -m venv Report/.venv
source Report/.venv/bin/activate
python -m pip install -r Report/requirements.txt
```

未建 `Report/.venv` 时，脚本会尝试项目根目录的 `.venv-xlsx` 或 `.venv`（需已安装 openpyxl，见 [Report/README.md](Report/README.md)）。

### 8.2 钉钉凭证

与 MCP `dingtalk-excel-read` 相同：`DINGTALK_AEGIS_KEY`、`DINGTALK_AEGIS_SECRET`、`DINGTALK_WORKID`。未设置环境变量时，`dingtalk_report_execute.py` 会尝试读取 `~/.cursor/mcp.json`。

### 8.3 验证

```bash
# 本地 xlsx
python3 Report/report_execute.py /path/to/v2.4.4版本用例.xlsx

# 钉钉 URL 一键拉表 + 生成（推荐）
python3 Report/dingtalk_report_execute.py "https://alidocs.dingtalk.com/i/nodes/XXXX"
```

在同目录（或桌面）生成 `{文件名}_内网测试总结.html` 与 `{文件名}_外网测试总结.html`。详见 [Report/README.md](Report/README.md)、[Report/使用方法.md](Report/使用方法.md)。

---

## 第九步：ADB 真机自动化（可选）

用于 Yaahlan App 真机 UI 操作、录制脚本回归与 Tunnel 抓包验收。

### 9.1 前置

- 安装 `adb`（Android platform-tools）  
- 手机开启 USB 调试，或 `adb connect <ip>:5555`  
- 目标 App 为 **Yaahlan**（`com.immomo.biz.yaahlan`），不是桌面 Yaha  

```bash
python3 adb/adb_execute.py devices    # 状态应为 device
python3 adb/adb_execute.py info
```

### 9.2 测试账号（登录前）

真机操作前先用 **空闲测试号**，一次只操作一个账号：

```bash
python3 adb/adb_execute.py accounts check --all          # 查看账号占用
python3 adb/adb_execute.py accounts login-idle --preferred familyLeader
# 登录指定号：macro 手机号登录 --text <手机号>
```

换号前须完成退出/收尾；勿用脚本批量扫多号（见仓库自动化规范）。

### 9.3 快速体验

```bash
python3 adb/adb_execute.py scripts                          # 按模块列出片段
python3 adb/adb_execute.py macro 手机号登录 --text 13311111115 --no-capture
python3 adb/adb_execute.py capture --max-edge 1170          # 截屏供 Agent 读图
python3 adb/adb_execute.py activity                         # 当前页快验
```

### 9.4 AI 读图与换机

首页 / Me / 房间 / 礼物等模块 **禁止盲跑 macro**，标准循环：

```bash
python3 adb/adb_execute.py ai prepare --goal enter_me   # logout / enter_room / recover 等
python3 adb/adb_execute.py capture --max-edge 1170
# Agent 读图 → tap X Y → activity 或 capture 验收
```

- 换机坐标：`adb/录制脚本/设备适配/README.md`（`device calibrate`）  
- 同一 macro **连续失败 3 次**会废弃，改 AI 读图 + Tunnel；`ai restore <片段名>` 可恢复  

**注意**：

- 多步流程用 **`macro A` → 验收 → `macro B`**，禁止长命令盲串联  
- `注册登录`、`动态`、`消息` 等仍可用 macro；`游戏`、`房间`、`礼物`、`个人主页` 等须 **AI 读图**  
- 真机已验收路径见 [`verified-kb/`](verified-kb/README.md)（与 `testcase-kb` 同名 md 内容互补）

详见 [adb/README.md](adb/README.md)、[adb/使用方法.md](adb/使用方法.md)、[adb/录制脚本/README.md](adb/录制脚本/README.md)、[adb/录制脚本/KB对照.md](adb/录制脚本/KB对照.md)。

---

## 常见问题

| 现象 | 原因 | 处理 |
|------|------|------|
| `缺少 Cookie` | 没有 `MOA/.env.local` 或未填 `MOA_COOKIE` | 按第三步配置 |
| 返回 HTML 登录页 | Cookie 过期 | 重新登录 MSE，更新 `MOA/.env.local` |
| `python3: command not found` | 未安装 Python 3 | 安装后重试 |
| 同事能用我不能用 | `.env.local` 不随 Git 同步 | 每人独立配置，不能复制别人的过期 Cookie |
| Admin 401 / 空数据 | `ADMIN_*` Token 过期或未填全 | 重新抓后台请求头（**sso-token** 与 **yaahlan-jwt** 均需） |
| MOA 业务 `ec:10011` | 体验 VIP 等级冲突等 | 先 `delVipInfo` 或发更高等级体验卡，见 MOA 使用方法说明 |
| 钉钉 MCP 鉴权失败 | `DINGTALK_COOKIE` 或 Aegis 失效 | 更新 `.cursor/mcp.json` 中 env → Reload Window |
| MCP 启动失败 / 找不到解释器 | Skill 目录下 **venv 未建** | 按 7.1 安装依赖并 Reload Window |
| parse_document 权限过期 | Cookie 失效 | 更新 Cookie 或 `refresh_cookie` 后重试 |
| Tunnel 无数据 | Cookie 不含 `tunnel_login_session` | 重新登录 MSE 更新 MOA Cookie，或单独配 `Tunnel/.env.local` |
| Report 报 `InvalidAuthentication` | Aegis 过期 | 更新 MCP env 或环境变量后重试 |
| Risk 解除失败 | 设备名不在 `test_devices.json` | `--list-test-devices` 查可用名，见 Risk README |
| `adb devices` 为空 / unauthorized | 未开 USB 调试或未授权 | 手机上点「允许 USB 调试」 |
| macro 被拒绝 | 该模块须 AI 读图 | `ai prepare` + `capture` + `tap`，或调试时加 `--force-script` |
| 测试号被占用 | 他人正在用同一账号 | `accounts pick` 换空闲号，或 `login-idle` |

---

## 自检清单（打印可勾选）

```
[ ] 已 git pull 到最新 yaahlan
[ ] 已创建 MOA/.env.local 且 MOA 查询 VIP 成功
[ ] 已创建 MOA/.venv（按需）且 test_all 或常用模板可用
[ ] 已创建 Admin/.env.local 且查询用户详情成功（如需）
[ ] Tunnel 抓包可返回请求列表 JSON（如需）
[ ] Risk `--list-test-devices` 可用（如需；无需 `.env.local`）
[ ] MCP venv 已安装（7.1）且 Cursor 中三个钉钉服务为绿色（如需）
[ ] 已确认 .cursor/mcp.json 钉钉 Cookie / Aegis 有效（如需生成用例）
[ ] 已运行 python3 scripts/doctor.py 通过（可选）
[ ] Report/.venv 已安装依赖且能生成 HTML（如需）
[ ] adb devices 可见 device；accounts login-idle 可登录（如需真机）
[ ] 已知悉：Cookie/Token 勿提交 Git；过期需自行更新
```

---

## 文档索引

| 文档 | 内容 |
|------|------|
| [README.md](README.md) | 项目总览、MCP、用例流程、各模块入口 |
| [SKILL.md](SKILL.md) | 营收活动用例自动生成主流程 |
| [MOA/使用方法.md](MOA/使用方法.md) | MOA 能力口令与命令（自动生成） |
| [MOA/README.md](MOA/README.md) | MOA 技术细节、登记新 MOA 流程 |
| [Admin/README.md](Admin/README.md) | 后台用户查询 |
| [Admin/使用方法.md](Admin/使用方法.md) | Admin 能力口令与命令（自动生成） |
| [Risk/README.md](Risk/README.md) | 风控接口 |
| [Risk/使用方法.md](Risk/使用方法.md) | Risk 命令速查 |
| [Tunnel/README.md](Tunnel/README.md) | HTTP 抓包查询 |
| [Tunnel/使用方法.md](Tunnel/使用方法.md) | Tunnel 命令速查 |
| [Report/README.md](Report/README.md) | 版本用例 xlsx → 测试总结 HTML |
| [Report/使用方法.md](Report/使用方法.md) | Report 命令速查 |
| [adb/README.md](adb/README.md) | ADB 设计原则、协作流程 |
| [adb/使用方法.md](adb/使用方法.md) | ADB 命令速查 |
| [adb/录制脚本/README.md](adb/录制脚本/README.md) | 录制脚本库与落库约定 |
| [adb/录制脚本/KB对照.md](adb/录制脚本/KB对照.md) | 知识库 ↔ 片段对照 |
| [testcase-kb/README.md](testcase-kb/README.md) | 用例知识库说明 |
| [bug-kb/README.md](bug-kb/README.md) | Bug 知识库说明 |
| [online-kb/README.md](online-kb/README.md) | 线上问题知识库说明 |
| [verified-kb/README.md](verified-kb/README.md) | 真机跑通功能验收知识库 |
| [rules/testcase_generation_rules.md](rules/testcase_generation_rules.md) | 通用用例生成规则（榜单/抽奖等） |
| [rules/version_testcase_generation_rules.md](rules/version_testcase_generation_rules.md) | 发版 / 版本回归用例规则 |
| [moments/README.md](moments/README.md) | 动态（Moments）用例生成流程 |

新增 MOA 能力后，维护者需更新 `MOA/config/registry.json` 并执行：

```bash
python3 MOA/scripts/generate_index.py
```

再提交 Git，其他同事 `pull` 即可看到更新后的 [MOA/使用方法.md](MOA/使用方法.md)。
