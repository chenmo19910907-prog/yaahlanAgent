# Yaahlan auto-generate-testcase · Hermes 项目上下文

工作目录必须是本仓库根目录。优先用下面入口，不要另造批量盲跑脚本。

## 知识库（只读参考）

| 目录 | 用途 |
|------|------|
| `testcase-kb/` | 版本用例提炼的产品规则 / 验收要点 |
| `prd-kb/` | 产品需求要点 |
| `bug-kb/` | 历史缺陷 |
| `documents/` | 模块业务说明 |
| `adb/录制脚本/` | ADB 片段（坐标积木，与 Midscene 互补） |

推荐先跑：`python3 scripts/suggest_kb_for_module.py <模块关键词>`

## 两条主链路

### A. 用例自动生成

1. 读 KB / PRD（见上）
2. 按根目录 `SKILL.md` 与 `rules/` 生成 Markdown 用例 → `temporary_testcase/`
3. `python3 scripts/check_testcase_md.py`
4. 需要时再导出钉钉 Excel

Skills：`/yaahlan-gen-testcase`

### B. Midscene 意图测试

1. 意图 YAML 在 `intent-test/intents/`
2. 从手工用例草稿：`cd intent-test && npm run md2intent -- ../temporary_testcase/<文件>.md`
3. 执行：`cd intent-test && npm run intent -- intents/<模块>/<文件>.yaml`
4. 步骤型回归仍用 `midscene/testcases-yaml/`（不要用意图层替代固定链路）

Skills：`/yaahlan-intent`

## 铁律

- 真机 ADB：一段一验，禁止不验收连跑 macro / 批量盲扫
- 含「线上环境」才用 `online/online_execute.py`；否则用测试环境入口
- 勿提交 `.env` / `.env.local` / Token
- 改模块 `registry.json` 后须跑对应 `generate_index.py`，勿手改 `platform/catalog.html`
