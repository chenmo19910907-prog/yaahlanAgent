## 已录入 MSE 清单（自动生成）

> 本文件由 `MSE/scripts/generate_index.py` 根据 `MSE/config/registry.json` 自动生成，请勿手动编辑。

### 能力清单

- [1) MSE 服务配置](#mse-cat-1)
  - [配置-Application 关键字过滤](#grep_application)
  - [配置-Application 按 key 读取](#get_application_config_key)
  - [配置-voga-activity 关键字过滤](#grep_voga_activity)
  - [配置-voga-activity 按 key 读取](#get_voga_activity_config_key)
  - [配置-关键字过滤](#grep_config_key)
  - [配置-列出 Application（私有）](#list_application)
  - [配置-列出 voga-activity](#list_voga_activity)
  - [配置-列出 voga-common](#list_voga_common)
  - [配置-按 key 读取](#get_config_key)

### 使用说明

- **提示词**：自然语言口令
- **命令**：可执行脚本（默认复用 `MOA/.env.local` 的 Cookie）
- **接口**：`POST /apirest/httpproxy/config/getConfigsByAppKeyAndNameSpace`
- **默认**：`appKey=momo.bpm.biz.gameplatform.overseas-voga-mts-vas`；常用 namespace：`voga-common`（公有通用）、`voga-activity`（公有活动）、`Application`（私有/application，API nameSpace 为空）
- **完整响应**：追加 `--output json`；单条值：`--config-key <key> --output value`

<a id="mse-cat-1"></a>

## 1) MSE 服务配置

<a id="grep_application"></a>

### 配置-Application 关键字过滤

- **功能**：在 Application（私有）namespace 中按 configKey 子串过滤
- **提示词**：
  - `Application 里查 gift 相关配置`
  - `grep 私有 application 礼物代言配置`
  - `私有配置里查 challenge 相关 key`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace Application --grep gift --limit 30
```

<a id="get_application_config_key"></a>

### 配置-Application 按 key 读取

- **功能**：在 Application（私有）namespace 按 configKey 精确读取单条服务配置
- **提示词**：
  - `读取 Application 配置 <configKey>`
  - `查 MSE 私有配置 giftSpokespersonTaskConfig`
  - `getConfigs Application giftSpokespersonTaskConfig`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace Application --config-key <configKey>
```

<a id="grep_voga_activity"></a>

### 配置-voga-activity 关键字过滤

- **功能**：在 voga-activity namespace 中按 configKey 子串过滤（活动相关配置 grep）
- **提示词**：
  - `voga-activity 里查 gift 相关配置`
  - `grep voga-activity 礼物代言配置`
  - `活动配置里查 valentine 相关 key`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-activity --grep gift --limit 30
```

<a id="get_voga_activity_config_key"></a>

### 配置-voga-activity 按 key 读取

- **功能**：在 voga-activity namespace 按 configKey 精确读取单条活动配置内容与说明
- **提示词**：
  - `读取 voga-activity 配置 <configKey>`
  - `查 MSE 活动配置 giftSpokespersonWhiteList`
  - `getConfigs voga-activity giftSpokespersonWhiteList`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-activity --config-key <configKey>
```

<a id="grep_config_key"></a>

### 配置-关键字过滤

- **功能**：在 namespace 结果中按 configKey 子串过滤（客户端 grep）
- **提示词**：
  - `voga-common 里查 family 相关配置`
  - `grep voga-common pk 配置`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-common --grep family --limit 30
```

<a id="list_application"></a>

### 配置-列出 Application（私有）

- **功能**：读取 vas 服务私有 application 配置清单（MSE 控制台「私有/application」；API nameSpace 为空，stage/alpha）
- **提示词**：
  - `读取 Application 私有配置列表`
  - `列出 MSE 私有 application 配置`
  - `查 stage 环境 vas 服务私有配置有哪些`
  - `搜索私有 application 配置`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace Application --limit 20
```

<a id="list_voga_activity"></a>

### 配置-列出 voga-activity

- **功能**：读取 vas 服务公有 namespace voga-activity（活动配置）下的配置清单（stage/alpha）；默认展示前 20 条 configKey
- **提示词**：
  - `读取 voga-activity 配置列表`
  - `列出 MSE voga-activity 公有活动配置`
  - `查 stage 环境 voga-activity 有哪些配置`
  - `搜索公有 voga-activity 配置`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-activity --limit 20
```

<a id="list_voga_common"></a>

### 配置-列出 voga-common

- **功能**：读取 vas 服务公有 namespace voga-common 下的配置清单（stage/alpha）；默认展示前 20 条 configKey
- **提示词**：
  - `读取 voga-common 配置列表`
  - `列出 MSE voga-common 公有配置`
  - `查 stage 环境 voga-common 有哪些配置`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-common --limit 20
```

<a id="get_config_key"></a>

### 配置-按 key 读取

- **功能**：按 configKey 精确读取单条配置内容与说明；支持任意 namespace/appKey
- **提示词**：
  - `读取 voga-common 配置 <configKey>`
  - `查 MSE 配置 key=<configKey>`
  - `getConfigs familyPkConfig`
- **命令**：

```bash
python3 MSE/mse_execute.py --namespace voga-common --config-key <configKey>
```
