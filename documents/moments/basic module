23453232



# 动态功能主要模块梳理

- 来源：桌面文件 `动态模块拆分.xlsx`（工作表「模块拆分」）
- 口径：按表格 **一级模块** 聚合，其下按 **二级模块** 分组罗列 **预期结果**，与源表一致，便于逐条对照验收。
- 关联：**动态视频发布与播放** 已从本文拆出，见同目录 **`视频发布`** 文件（源：同一 Excel 工作表 **Sheet1**）。
- 说明：原表中「超管审核 tab」相关行在「一级」列存在 `入口` / `审核tab` / `操作审核` 等分段，本文已合并为同一一级 **超管动态审核（审核tab）**；「站内巡查」首行说明误写在 B 列时，归入 **基础说明**。

## 主要模块

### 1. 动态列表页

- **模块范围（二级）**：`入口`、`顶部tab`、`动态发布【➕】按钮`、`动态列表`、`动态用户信息`、`关注按钮`、`动态发布时间`、`动态文字区域`、`动态图片区域`、`动态列表页评论按钮`、`动态列表页点赞按钮`、`动态列表页送礼按钮`、`【...】按钮`
- **验收要点（按二级）**：

#### 入口
- 点击底部动态帧，默认进入发现tab

#### 顶部tab
- 展示发现tab，关注tab
- 发布tab默认被选中，高亮展示
- tab间能来回切换，被选中的tab高亮展示
- 从其他帧切回到动态帧回到原先的tab

#### 动态发布【➕】按钮
- 动态发布按钮展示在右上角
- 点击进入动态发布页

#### 动态列表
- 展示全部用户发布的动态
- 新注册用户列表正常展示
- 动态为空展示文案：暂无动态，去发现页看更多动态吧（关注tab）
- 按照发布时间，从近到远排序
- 列表上下滑动流畅
- 下拉到最后一条动态时，不会被底部导航栏遮挡
- 下拉刷新列表
- 切换底部tab刷新列表
- 发现tab和关注tab切换时不刷新
- 从动态详情页返回动态列表页时，不刷新列表
- 滑动列表到中间位置，点击动态帧tab，会返回列表顶部

#### 动态用户信息
- 展示用户头像，昵称，性别
- 特权用户展示对应的头像框
- 用户在语聊房，且房间是公开状态时，头像外展示波纹效果
- 昵称超过一行打点省略
- 昵称包含表情/特殊符号要正常展示
- 头像波纹状态下，点击进入语音房（点击用户信息区域）
- 点击时切换为私密房，toast提示该房间为私密房，仅限管理员邀请进入
- 点击时房间已解散，toast提示该房间已解散
- 头像正常状态下，点击进入个人资料页（点击用户信息区域）
- 用户信息修改后，刷新列表信息会更新

#### 关注按钮
- 发现tab，未关注的用户展示关注按钮，点击后按钮消失
- 发现tab，已关注的用户不展示关注按钮
- 用户点击关注后，此时切换到关注tab，手动刷新，关注列表增加新关注用户动态展示；
- 列表页点击关注后进入动态详情页，动态详情页不展示关注按钮
- 动态详情页点击关注按钮后，返回列表页不展示关注按钮

#### 动态发布时间
- 用户信息下方展示发布时间
- 当地时间当日发布，显示：发布时间，举例：03:15
- 昨日发布，显示：昨天,发布时间，举例：昨天,03:15
- 除以上两种情况，显示：日/月,发布时间，举例：  31/08,03:15

#### 动态文字区域
- 列表最多展示3行文字，超过3行打点省略，并展示“更多”按钮
- 点击更多按钮跳转动态详情页
- 点击文字区域进入动态详情页
- 图文动态展示正常
- 纯图片动态展示正常
- 纯文本动态展示正常
- 中文，英文，阿语，表情，特殊符号，换行展示正常
- 开头空格不省略，末尾空格省略

#### 动态图片区域
- 最多展示9张图
- 图片顺序与发布器内的顺序一致
- 单图300✖️300展示
- 多图，展示正方形缩略图，2，3，4，5，6，7，8，9
- 点击图片查看大图
- 点击返回按钮，返回动态列表页
- 多图的动态，在大图状态下能左右滑动切换图片

#### 动态列表页评论按钮
- 展示评论数+评论回复数
- 无评论时显示为0
- 评论数大于0但小于等于99时展示具体数字
- 评论数大于99时展示99+
- 点击评论按钮进入动态详情页，键盘自动弹出
- 动态列表评论数字与动态详情页评论数字一致

#### 动态列表页点赞按钮
- 展示点赞数
- 无点赞时显示为0
- 点赞数大于0但小于等于99时展示具体数字
- 点赞数大于99时展示99+
- 未点赞状态下，点击按钮，点赞数+1，图标变为已点赞状态
- 已点赞状态下，点赞按钮，点赞数-1，图标变为未点赞状态
- 动态详情点赞数字与动态列表页点赞数字一致
- 他人点赞后，自己需要刷新列表才更新点赞数
- 自己点赞后，点赞数+1

#### 动态列表页送礼按钮
- 展示送礼数
- 无送礼时显示为0
- 送礼数大于0但小于等于99时展示具体数字
- 送礼数大于99时展示99+
- 未送礼状态下，图标为未送礼状态
- 已送礼状态下，图标为已送礼状态
- 详情点赞数字与动态列表页送礼数字一致
- 点击礼物按钮，进入动态详情页礼物tab
- 送礼后，送礼数+1

#### 【...】按钮
- 主态点击展示【删除】【取消】选项弹窗
- 点击【删除】后，弹出二次确认弹窗，
- 弹窗文案：删除动态后，点赞、评论等互动数据也将一起清空，确认删除吗？
- 点击确定后，该条动态从列表中删除
- 其他用户刷新列表后，该条动态不再展示
- 其他用户点击已删除的动态，在列表页，弹toast：该动态已删除，无法查看详情。
- 点击【取消】后，弹窗收起
- 客态点击展示【举报】【取消】选项弹窗
- 点击【举报】后，弹窗收起，走举报流程
- 举报后台能看到相关信息
- 点击【取消】后，弹窗收起
- 超管态点击展示【删除】【取消】选项弹窗
- 点击【删除】后，弹出二次确认弹窗
- 弹窗文案：删除动态后，点赞、评论等互动数据也将一起清空，确认删除吗？
- 点击确定后，该条动态从列表中删除
- 其他用户刷新列表后，该条动态不再展示
- 其他用户点击被屏蔽的动态，在列表页，弹toast：该动态已删除，无法查看详情。
- 删除后小助手下发消息通知，文案你发布的：您发送的“{评论内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。


### 2. 动态详情页

- **模块范围（二级）**：`入口`、`顶部`、`动态用户信息`、`关注按钮`、`动态发布时间`、`动态文字区域`、`动态图片区域`、`评论tab`、`评论列表`、`评论用户信息`、`评论时间`、`评论内容区域`、`评论回复`、`评论举报/删除`、`送礼`、`点赞tab`、`点赞列表弹窗`、`点赞用户信息`、`输入框`、`动态详情页点赞按钮`、`动态详情页【...】按钮`
- **验收要点（按二级）**：

#### 入口
- 动态帧点击自己的动态进入动态详情页
- 自己的个人资料页点击发布过的动态

#### 顶部
- 标题：动态详情
- 返回按钮，点击返回上级页面

#### 动态用户信息
- 展示用户头像，昵称，性别
- 特权用户展示头像框
- 用户在语聊房，且房间是公开状态时，头像外展示波纹效果
- 昵称超过一行打点省略
- 头像波纹状态下，点击进入语音房（点击用户信息区域区域）
- 头像正常状态下，点击进入个人资料页（点击用户信息区域）
- 用户信息要与列表页用户信息一致

#### 关注按钮
- 动态主态不展示关注按钮
- 未关注的用户展示关注按钮，点击后按钮消失
- 已关注的用户不展示关注按钮
- 列表页点击关注后进入动态详情页，动态详情页不展示关注按钮
- 动态详情页点击关注按钮后，返回列表页不展示关注按钮

#### 动态发布时间
- 用户信息下方展示发布时间
- 当地时间当日发布，显示：发布时间，举例：03:15
- 昨日发布，显示：昨天,发布时间，举例：昨天,03:15
- 除以上两种情况，显示：日/月,发布时间，举例：  31/08,03:15
- 发布时间要与列表页动态发布时间一致

#### 动态文字区域
- 展示动态的全部文案
- 超长文案动态正常展示
- 图文动态展示正常
- 纯图片动态展示正常
- 纯文本动态展示正常
- 中文，英文，阿语，表情，特殊符号，空格，换行展示正常

#### 动态图片区域
- 最多展示9张图
- 图片顺序与发布器内的顺序一致
- 单图等比例缩放
- 多图，展示正方形缩略图，2，3，4，5，6，7，8，9
- 点击图片查看大图
- 大图状态下点击返回按钮，返回动态列表页
- 多图的动态，在大图状态下能左右滑动切换图片

#### 评论tab
- 主态展示评论tab和点赞tab
- 默认选择评论tab，高亮展示
- 展示评论数+评论回复数。无评论显示0，评论数最大999，超过显示999+

#### 评论列表
- 评论列表按照评论时间，从近到远排序
- 评论列表上下滑动流畅
- 评论为空时展示文案：暂无评论
- 最底部的评论不会被输入框遮挡
- 他人发布评论后，自己需要刷新列表才展示，评论数+1，新评论
- 他人回复评论后，自己需要刷新列表才展示，评论数+1，新回复
- 自己发布评论后，评论数+1，展示新评论
- 自己评论回复后，评论数+1，展示新回复

#### 评论用户信息
- 展示用户头像，昵称，性别
- 特权用户展示对应的头像框
- 昵称超过一行打点省略
- 点击进入个人资料页（点击用户信息区域）
- 用户信息修改后，刷新列表信息会更新

#### 评论时间
- 用户信息下方展示发布时间
- 当地时间当日发布，显示：发布时间，举例：03:15
- 昨日发布，显示：昨天,发布时间，举例：昨天,03:15
- 除以上两种情况，显示：日/月,发布时间，举例：  31/08,03:15

#### 评论内容区域
- 中文，英文，阿语，表情，特殊符号，空格，换行展示正常
- 长评论（300字）展示正常

#### 评论回复
- 点击评论回复按钮弹起键盘，对评论进行回复
- 回复评论后，评论数字+1
- 一条评论被多次回复，按照回复时间，从近到远排序。
- 回复评论默认展示一条，其他折叠显示文案“展开更多回复”
- 点击“展开更多回复”展示所有的评论回复
- 点击评论回复用户的进入个人资料页（点击用户信息区域）
- 评论回复用户的信息修改后，刷新列表评论回复用信息会更新
- 评论和评论回复支持点赞操作
- 无点赞时显示为0
- 点赞数大于0但小于等于99时展示具体数字
- 点赞数大于99时展示99+
- 未点赞状态下，点击按钮，评论点赞数+1，图标变为已点赞状态
- 已点赞状态下，点赞按钮，评论点赞数-1，图标变为未点赞状态

#### 评论举报/删除
- 超管身份长按评论/回复，展示【删除】【取消】选项弹窗
- 点击删除，弹窗消失，弹出二次确认弹窗
- 二次弹窗点击确认后，弹窗消失，该条评论从评论列表消失，总评论数减去评论数+回复数
- 评论删除，评论的回复也会被删除
- 评论删除后，用小助手官方号发送警告文案： 您发送的“{评论内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。
- 点击取消，弹窗消失
- 动态发布者长按自己动态下评论/回复，展示【删除】【取消】选项弹窗
- 点击删除，同上
- 点击取消，弹窗消失
- 评论者/评论回复者长按自己发布的评论/回复，展示【删除】【取消】选项弹窗
- 点击删除，同上
- 点击取消，弹窗消失
- 其余情况下，长按展示【举报】【取消】选项弹窗
- 点击举报后走举报流程
- 举报后台能看到相关举报信息

#### 送礼
- 可以切换送礼tab
- 展示送礼数
- 无送礼时显示为0
- 送礼数大于0但小于等于99时展示具体数字
- 送礼数大于99时展示99+
- 未送礼状态下，图标为未送礼状态
- 已送礼状态下，图标为已送礼状态
- 详情点赞数字与动态列表页送礼数字一致
- 点击礼物按钮，进入动态详情页礼物tab
- 快捷送礼可以送礼成功
- 礼物面板送礼可以送礼成功
- 钻石不足时送礼跳转充值页面
- 送礼后，送礼数+1

#### 点赞tab
- 文案和数字高亮展示
- 点击弹出点赞列表弹窗
- 展示动态的点赞数。无点赞时，展示为0，评论数最大999，超过显示999+
- 评论/评论回复的点赞数不计算在内

#### 点赞列表弹窗
- 点赞列表按照评论时间，从近到远排序
- 点赞列表上下滑动流畅
- 点赞列表为空时展示文案：暂无点赞还没有收到点赞
- 点击页面其他区域，弹窗消失
- 他人点赞后，自己需要刷新列表才展示，评论数+1，新点赞消息
- 自己点赞后，点赞数+1，新点赞消息

#### 点赞用户信息
- 展示用户头像，昵称，性别
- 特权用户展示对应的头像框
- 昵称超过一行打点省略
- 点击进入个人资料页（点击用户信息区域）
- 用户信息修改后，刷新列表信息会更新

#### 输入框
- 底部展示输入框+发送按钮
- 输入框默认展示文案：留下一条友好的评论吧
- 输入内容后默认展示文案消失
- 内容删除后重新展示默认文案
- 点击输入框弹起键盘
- 点击其他区域键盘收起
- 评论/评论回复字数限制300
- 超出字数限制时，点击发送按钮弹出toast“超过300字数限制”
- 评论发送后，键盘自动收起
- 中文，英文，阿语，表情，特殊符号，换行的评论/回复能正常发送
- 开头空格不省略，末尾空格省略
- 输入框支持复制/粘贴功能
- 输入框输入内容后，收起键盘，内容会保留在输入框
- 输入框内有内容，进入其他页面再返回，输入框内容保存（页面销毁，内容不保存）
- 键盘弹起状态，点击发送按钮能发送评论
- 键盘收起状态，点击发送按钮能发送评论
- 文本审核：色情（色情敏感词）、云上曲率-涉政、云上曲率-未成年、云上曲率-辱骂、云上曲率-色情、云上曲率-仇恨言论
- 评论命中模型，评论内容仅自己可见，用小助手官方号给用户发送文案提示：您发送的“{评论内容}”不符合社区规范，请及时删除。（敏感词库？）

#### 动态详情页点赞按钮
- 展示点赞数
- 无评论时显示为0
- 点赞数大于0但小于等于99时展示具体数字
- 点赞数大于99时展示99+
- 未点赞状态下，点击按钮，点赞数+1，图标变为已点赞状态
- 已点赞状态下，点赞按钮，点赞数-1，图标变为未点赞状态
- 动态详情点赞数字与动态列表页点赞数字一致
- 他人点赞后，自己需要刷新列表才更新点赞数
- 自己点赞后，点赞数+1，新点赞消息

#### 动态详情页【...】按钮
- 发布者点击展示【删除】【取消】选项弹窗
- 点击【删除】后，弹出二次确认弹窗
- 弹窗文案：删除动态后，点赞、评论等互动数据也将一起清空，确认删除吗？
- 点击确定后，，该条动态从列表中删除
- 其他用户刷新列表后，该条动态不再展示
- 其他用户点击已删除的动态，在列表页，弹toast：该动态已删除，无法查看详情。
- 普通用户点击展示【举报】【取消】选项弹窗
- 举报完成后，返回上级页面
- 点击【取消】后，弹窗收起
- 点击【删除】后，弹出二次确认弹窗，该条动态从列表中删除
- 弹窗文案：删除动态后，点赞、评论等互动数据也将一起清空，确认删除吗？
- 点击确定后，，该条动态从列表中删除
- 其他用户刷新列表后，该条动态不再展示
- 其他用户点击被删除的动态，在列表页，弹toast：该动态已删除，无法查看详情。
- 删除后小助手下发消息通知，文案你发布的：您发送的“{评论内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。


### 3. 动态发布

- **模块范围（二级）**：`入口`、`编辑页展示编辑光标，自动弹出软键盘`、`支持内容的类型`、`发布按钮状态`、`文本输入的规则`、`发布动态-接入敏感词`、`图片选择逻辑`、`图片选择-勾选状态`、`图片选择-完成按钮可点击`、`上传图片`、`动态发布逻辑`、`发布中-页面不可操作`、`发布成功`、`发布失败-toast出现周期的处理`、`异常场景-弱网`、`前后台切换`、`图片选择-权限取消`、`顶号`、`小窗`、`发动态编辑-屏蔽邀请`、`对于已经删除的动态处理`、`审核不通过动态、评论处理`、`超管名单`
- **验收要点（按二级）**：

#### 入口
- 切换到动态tab-右上角「+」
- 点击关注帧/发现帧右上角「+」，进入发布动态页

#### 编辑页展示编辑光标，自动弹出软键盘
- 未输入任何内容有默认文案
- 输入内容，展示内容，清空内容回复展示展示默认文案
- 软键盘支持复制粘贴，注意ios有复制粘贴权限弹窗提示

#### 支持内容的类型
- 动态支持图文、纯图片「jpg、png」、纯文本（支持RAW、livephoto）
- 发布后支持删除动态
- 暂不支持动态编辑

#### 发布按钮状态
- 当前未输入任何内容或者没有上传任何图片时，发布按钮置灰且不可点击
- 只输入空格，发布按钮置灰不可发布
- 输入内容或者上传图片，有任意一种，发布按钮解除置灰状态，可以点击发布

#### 文本输入的规则
- 当前输入文本内容，不展示实时字数检测，发布的时候提示，设计补计数器？
- 字数最大限制到1000个字符
- 如果输入的是长文本，光标展示在最后一行
- 输入的文本开头有空格或者文本中间有空格，保留空格。仅去除末尾的空格
- 文本内容支持纯文字、emoji和颜文字，且支持换行，不同机型显示emoji、颜文字会有偏差
- 文字编辑发送支持换行功能

#### 发布动态-接入敏感词
- 发布的动态：图文、纯文字、图片，接入敏感词库，触发敏感词库，toast提示，图片异步检测，异常：审核失败

#### 图片选择逻辑
- 支持图库和拍照，图库需要对应权限，拍照需要相机权限，拒绝需要手动开启
- 编辑页点击相机图标弹出菜单，选择图库和相机
- 图库图片选择支持小图勾选和大于预览勾选，注意格式以及大文件内存溢出问题
- 预览状态下底部展示图片数量，最多支持9张图，格式：0/9
- 勾选超过9张toast提示：最多选择9张图

#### 图片选择-勾选状态
- 在发布动态编辑阶段，选择图片勾选会有勾选的状态变化。UI会变化

#### 图片选择-完成按钮可点击
- 在图片选择阶段，右上角的完成按钮始终可点击。
- 用户不勾选图片点击完成直接回到发布编辑页。

#### 上传图片
- 相册页选择完成后进入上传流程，分辨率保持
- 上传完成后，回到编辑页，可以点击查看大图
- 小图模式支持删除，点击查看大图则不支持删除
- 删除图片：点击小图icon「X」，即可删除
- 图片排列顺序是3*3，不支持拖动改变顺序
- 删除了图片，小于9张后，支持后续继续选择图片

#### 动态发布逻辑
- 动态暂无草稿箱逻辑
- 在编辑页点击右上角发布按钮，进入发布流程
- 点击发布按钮停留在编辑页，展示loading
- 动态未发布完成时，在个人帧展示发布中-最右侧的区域

#### 发布中-页面不可操作
- 在编辑页输入内容后点击发布，展示发布中的loading动效，页面不可操作
- 文案是：发布中，请稍等

#### 发布成功
- 在发布编辑页点击发布按钮后发布成功了，弹出toast：发布成功
- toast消失后，跳转至个人动态页。

#### 发布失败-toast出现周期的处理
- 当前动态发布失败出现toast提示的时候，页面任何区域均无法点击
- toast文案：发布失败，请重新发布
- 等toast消失后，页面所有区域恢复点击

#### 异常场景-弱网
- 动态编辑页输入好内容点击发布，网络波动，超时提示发布失败，支持重试
- 发布中的loading超时机制：iOS 一次请求时间是30S，android保持旧的逻辑

#### 前后台切换
- 前后台切换-同上场景，在发布的过程中前后台切换，正常在发布。
- 动态编辑页弹出软键盘，前后台切换注意键盘状态，SANSUNG容易出bug
- 同上场景，发布中切后台收到push，点击直接跳转，保持旧逻辑

#### 图片选择-权限取消
- 进入图片选择阶段，进入图库，切后台app设置图库权限取消，切回来APP应该回到首页
- 进入图片选择拍摄，进入相机拍摄阶段，切后台app设置相机权限取消，切回来APP应该回到首页

#### 顶号
- 发布的时候遇到账号被顶了，未发布好的时候，消息丢失，没有走重试逻辑

#### 小窗
- 发布动态为纯文本类型，正常发布
- 如果发布的内容涉及图片、相机支持才可以继续操作「ios」，Android不受影响

#### 发动态编辑-屏蔽邀请
- 正在动态编辑的时候，所有邀请需屏蔽：游戏、语音房

#### 对于已经删除的动态处理
- 当前用户已经刷到的动态被删除后，用户正常查看
- 下拉刷新该动态消失。
- 用户在列表点击已经删除的动态，进入详情页toast提示：动态已删除，无法查看详情
- 互动列表处理：已经产生过互动的动态，当动态被删除，互动列表正常展示，点击跳转的时候toast提示：该动态已删除，无法查看详情

#### 审核不通过动态、评论处理
- 用户发布发动态，内容包含色情、敏感词发布后命中模型，小助手号给用户下发IM消息通知
- 文案格式：{评论内容}+不符合社区规范，请及时删除
- 命中的动态仅自己可见，不分发，用户查看你个人页，也无法看到。
- 文字和图片审核以及评论均走对应模型
- 动态发布后走审核流程。 审核中：仅自己可见  审核结果为危险：显示不通过，动态被屏蔽，小助手官方号下发IM消息通知  审核结果为通过，动态变为全部人可见。

#### 超管名单
- 屏蔽的类型：动态、动态评论、语音房
- 如果当前账号是超管名单里的，点击「…」，弹出删除按钮，选择屏蔽跳转二级页面--见动态详情页case
- 可以选择类型提交，屏蔽的动态只能自己可见，屏蔽后会给该用户下发一条系统通知
- 文案格式：您发送的“{内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。图片动态文案不区分
- 文案： 您发送的“{评论内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。


### 4. profile动态入口

- **模块范围（二级）**：`动态入口-主态`、`动态入口-主态-入口展示规则`、`动态入口-主态-新旧版本互动`、`个人帧动态入口-客态`、`个人动态列表展示`、`异常冗余场景-前后台切换`、`弱网`、`crash/强杀`、`顶号`
- **验收要点（按二级）**：

#### 动态入口-主态
- 个人页钱包入口切换到设置按钮左侧
- 抢车位入口悬浮窗展示在大图头像右侧，支持goto跳转 待确认是否加入
- 增加动态入口：展示在语音房入口上方

#### 动态入口-主态-入口展示规则
- 如果当前用户发布过动态，展示对应动态缩略图
- 缩略图取首张图片，纯文字展示前两行的文字，不足展示全部文字，超过展示… 注意android机型适配
- 最多支持展示4个，超过四个，展示最新的4个
- 可以是纯文字、图文以及图片的动态
- 缩略图后面有发动态的「+」，点击走发布动态逻辑
- 没发布过动态，只展示「+」
- 点击任意一个动态缩略图，跳转至我的动态列表页面，支持查看所有的动态。
- 个人动态列表页面内容超过3行，展示更多按钮，点击展开查看全部内容。

#### 动态入口-主态-新旧版本互动
- 旧版本用户在自己的个人页，无法使查看到动态入口
- 旧版本升级到新版本，在个人页展示动态入口
- 新版本发布过动态卸载安装回老版本，个人页隐藏掉入口

#### 个人帧动态入口-客态
- 进入一个用户的个人帧后，用户未发布过动态，展示文案：他还没有发布过动态-区分男女
- 用户发布过动态，展示最多5个缩略图动态，右侧展示「更多」
- 点击任意一个，进入该用户的个人动态列表
- 个人动态列表页面内容超过3行，展示更多按钮，点击展开查看全部内容。

#### 个人动态列表展示
- 不管是主态还是客态，只要动态有点赞和评论，都会展示对应数字，数字上限999+
- 自己点过赞的动态，红心展示为红心，反之展示空心
- 当前为主态，点击动态「…」，展示删除和取消按钮，点击删除动态消失。二次确认
- 当前为客态，点击动态「…」，展示举报和取消按钮，点击举报复用线上举报逻辑。
- 举报入口：客态推荐、关注页、每条动态右上角、详情页右上角、个人动态列表
- 超管名单：如果当前账号是超管名单里的，点击「…」，弹出屏蔽和取消按钮，选择屏蔽跳转二级页面
- 可以选择类型提交，屏蔽的动态只能自己可见，屏蔽后会给该用户下发一条系统通知
- 文案格式：你发布的「动态前10个字」因违反社区规范被限流，请及时删除动态内容 图片动态文案是不是要区分下？

#### 异常冗余场景-前后台切换
- 主态视角入口，钱包移动到设置按钮左侧，动态入口展示在语音房上方。
- 客态视角入口，未发布展示缺省文案，切换到前台是手动更新
- 客态视角入口，发布过动态就展示动态，最多展示五个，超过显示更多

#### 弱网
- 进入个人帧、个人动态列表的时候，网络波动导致超时，页面展示缺省文案，旧样式

#### crash/强杀
- 进入正常展示

#### 顶号
- 停留在个人动态列表页、个人帧，顶号成功后，老设备进入注册/登陆页，新设备进入首页
- 所有旧设备未提交的操作，全部回滚


### 5. 互动通知

- **模块范围（二级）**：`入口`、`通知类型`、`互动通知外显cell`、`互动通知列表`、`互动通知列表-互动时间`、`互动通知列表-互动时间-时间格式`、`互动通知列表-互动时间-push相关`、`前后台切换`、`回收/crash`、`弱网/无网`、`顶号场景`
- **验收要点（按二级）**：

#### 入口
- 展示在消息帧，新增一个通知消息盒子，展示数字气泡
- 消息盒子位置不固定，根据消息接收时间展示
- 消息帧右上角个人帧，不展示数字气泡

#### 通知类型
- 动态被点赞
- 动态收到评论
- 评论被点赞
- 发布的评论被回复

#### 互动通知外显cell
- 当前只有一条就展示该条互动通知，区分通知类型
- 多条通知，展示最新的一条互动通知外显，多条数字气泡自增，气泡上限99+
- 点击过的互动通知，数字气泡消失

#### 互动通知列表
- 在消息帧点击互动通知跳转至互动通知列表
- 列表展示对应互动通知的用户头像、昵称、性别
- 整条支持点击跳转至详情页
- 副标题展示通知的类型，根据类型展示对应的文案「文本、图文、图片，根据类型展示」
- 副标题的文案过长，超过的部分展示省略号，最多展示2行--注意小屏幕机型适配
- 如果被回复的消息是图文或者图片，在通知列表最右侧展示图片缩略图
- 如果被回复的动态是纯文本，最右侧不展示缩略图
- 支持删除单条互动通知，暂不支持
- 互动通知列表有手动刷新：无

#### 互动通知列表-互动时间
- 在互动通知列表展示对应互动的时间

#### 互动通知列表-互动时间-时间格式
- 时间格式：当天：HH：MM
- 隔天：昨天，HH：MM
- 超过2天：DD/MM,HH:MM，跨年不展示年
- 排序规则：按照时间先后顺序，最新的展示在最前

#### 互动通知列表-互动时间-push相关
- 收到点赞数+评论数超过30次/H，
- 超过之后点赞通知不下发，当日晚上11点统一下发push，未读的消息 时区：东三区
- push格式： title：yaahlan 副标题：你有X条未读互动消息「X为变量」  服务端控制版本号
- 产生互动的动态消息，比如回复、点赞、评论等，当动态被删除后，点击跳转应提示动态已删除
- 对于push下发的互动消息，原动态被删除，点击后也是提示动态已删除，返回上一级页面

#### 前后台切换
- 互动通知列表有互动的消息时，前后台切换，页面数据展示正常
- 停留在消息tab下，互动通知消息盒子数字气泡未被点击，前后台切换，自增数字气泡数实时更新
- 消息tab个人帧右上角有新关注的消息应展示自增

#### 回收/crash
- 停留在互动通知列表锁屏回收，再次进入该页面应该自动刷新，不能白屏 -注意VIVO、Samsung机型

#### 弱网/无网
- 互动通知列表进入遇到网络波动请求失败了，文案：网络错误，旧逻辑

#### 顶号场景
- 账号被顶了，互动消息列表的数据在新设备上，存在服务器上
- 被顶的设备账号退出了，原本下发的push消息点击后不跳转


### 6. 站内巡查（超管）

- **模块范围（二级）**：`基础说明`、`语音房`、`动态`
- **验收要点（按二级）**：

#### 基础说明
- 由服务端白名单添加“巡查”管理身份

#### 语音房
- 权限：巡查>房主>管理>普通用户
- 巡查点开成员列表页可对列表中除自己外的所有用户进行操作踢出房间
- 操作踢出房间时进行弹窗二次确认
- 踢用户被踢时的前端提示与被房主、管理踢出时保持一致
- 被踢用户12小时内无法进入被踢语音房（包括房主本人）
- 房主不可以踢超管，踢出提示：网络错误

#### 动态
- 巡查点击动态【更多】时，包含【删除】选项
- 点击【删除】，直接删除该条
- 屏蔽后的动态不在动态的【发现】和【关注】中分发
- 还可以删除评论
- 文案格式：您发送的“{内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。图片动态文案不区分
- 文案： 您发送的“{评论内容}”不符合社区规范，已被管理员删除。请注意您的言辞，维护友好的社区氛围。


### 7. 动态帧红点

- **模块范围（二级）**：`触发条件`、`消失条件`
- **验收要点（按二级）**：

#### 触发条件
- 关注的人有新动态，用户冷启动后，触发关注tab红点，动态帧透出红点。
- 用户在站内时，关注的人有新动态，不触发红点；

#### 消失条件
- 用户进入关注tab页面，关注tab红点、动态帧红点实时消失。


### 8. 超管动态审核（审核tab）

- **模块范围（二级）**：`超管身份的动态帧`、`公会长身份的动态帧`、`主播身份的动态帧`、`普通用户身份的动态帧`、`客服身份的动态帧`、`列表展示`、`审核列表中的动态`、`审核通过`、`状态生效前`、`状态生效后`、`操作其他超管操作过的审核提示`、`操作其他超管操作过的审核但是在60s之内`、`未审核的动态`、`对于选择【通过】的动态`、`对于选择【删除】的动态`、`条件`、`审核后`
- **验收要点（按二级）**：

#### 超管身份的动态帧
- 新增审核tab--在关注后面

#### 公会长身份的动态帧
- 不新增

#### 主播身份的动态帧
- 不新增

#### 普通用户身份的动态帧
- 不新增

#### 客服身份的动态帧
- 不新增

#### 列表展示
- 取消关注按钮
- 取消点赞按钮
- 取消评论按钮
- 取消送礼按钮
- 取消更多按钮
- 增加通过按钮
- 增加删除按钮
- 纯图片动态
- 图文动态
- 带语音房链接的动态---实现为主

#### 审核列表中的动态
- 点击可进入动态详情页
- 点头像可进入profile页

#### 审核通过
- 60s后审核状态生效

#### 状态生效前
- 可点击切换审核状态
- 切换状态后，重新进行60s倒计时

#### 状态生效后
- 点击切换审核状态，tost提示【审核已生效，无法进行修改】

#### 操作其他超管操作过的审核提示
- tost提示：【该动态已被其他Admin审核】
- 更新为对方所审核的选项

#### 操作其他超管操作过的审核但是在60s之内
- 操作相同的选项
- tost提示：【该动态已被其他Admin审核】
- 操作不同的选项
- tost提示：【该动态已被其他Admin审核】

#### 未审核的动态
- 仅在用户的profile页的动态列表中可见

#### 对于选择【通过】的动态
- 生效后可正常在【推荐】和【关注】帧进行展示---外网不上推荐，只有关注有

#### 对于选择【删除】的动态
- 生效后走超管删除动态的逻辑
- im消息下发提示

#### 条件
- 最近7天，有过被超管删除动态案底的用户所发送的所有动态（未经审核）
- 按照发送时间的正序进行排列
- 发布早的在上面，晚的在下面

#### 审核后
- 审核后列表不进行实时的刷新
- 下滑进行分页加载---20条一页
- 从下往上回滑后依然能看到本次所有审核过的动态
- 手动下拉刷新或切帧刷新
- “推荐”和“关注”帧的动态排序由之前按照发布时间倒序调整为按照“可见”时间倒序进行排序
- 免审动态的可见时间=发布时间


## 总结

- 共 **8** 个一级模块、**99** 个二级分组、**441** 条预期结果（含从 B 列回收的说明类文案）。**动态视频** 验收见同目录 **`视频发布`**。
- 主干链路可概括为：**列表/详情浏览 → 发布 → Profile 入口 → 互动通知 → 站内巡查 → 超管动态审核 → 帧红点**，迭代时建议按一级模块做回归切片；视频能力见 **`视频发布`** 文末总结。
