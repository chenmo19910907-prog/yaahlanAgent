## 已录入工作流清单（自动生成）

> 本文件由 `workflow/scripts/generate_index.py` 根据 `workflow/config/registry.json` 自动生成，请勿手动编辑。

### 目录

- [1) 3周年活动](#workflow-cat-1)
  - [3周年砸金蛋数据测试（六步总览）](#anniversary_egg_data_test)
  - [3周年砸金蛋-造数并记录](#anniversary_egg_smash_record)
- [2) MOA-generative](#workflow-cat-2)
  - [MOA-generative-抓包转MOA执行](#moa_generative_run)
- [3) 亲密关系](#workflow-cat-3)
  - [亲密关系-结CP](#intimate_cp_form)
  - [亲密关系-结挚友](#intimate_buddy_form)
- [4) 家族PK](#workflow-cat-4)
  - [家族PK数据测试（新表+六步总览）](#family_pk_data_test)
  - [家族PK配置-新建测试钉钉表](#family_pk_config_create_workbook)
  - [家族PK配置-MSE同步到钉钉参数表](#family_pk_config_mse_to_dingtalk)
  - [家族PK配置-后台家族成员写入钉钉](#family_pk_config_pk_list_to_dingtalk)
  - [家族PK配置-收礼榜造数与档位测算](#family_pk_config_rank_rematch)
  - [家族PK配置-重匹配与验收写入钉钉](#family_pk_config_match_verify)
  - [家族PK配置-成员PK造数与发钻测算](#family_pk_config_member_pk_reward)
  - [家族PK配置-发奖与验收](#family_pk_config_dispatch_verify)
  - [家族PK配置-测试结果汇总](#family_pk_config_test_result)
  - [家族PK配置-档位PK与奖励测算](#family_pk_config_reward_calc)
  - [家族PK配置-钉钉参数表生成JSON](#family_pk_config_sheet_to_json)
  - [家族PK-重置匹配并下发前日奖励](#family_pk_daily_rematch)
- [5) 房间](#workflow-cat-5)
  - [房间成员-快速添加](#room_member_quick_add_form)
- [6) 抓包](#workflow-cat-6)
  - [Tunnel 抓包常用验收](#tunnel_capture_common)
- [7) 背包礼物](#workflow-cat-7)
  - [背包礼物-Tunnel背包验收](#package_gift_backpack_verify)
  - [背包礼物-下发与Tunnel验收](#package_gift_dispatch_verify)

### 使用说明

- **录制**：Agent 描述多步流程后写入 `workflow/workflows/<id>.json`，执行 `record` 落库
- **复用**：`python3 workflow/workflow_execute.py run <id> --参数 ...`
- **参数**：工作流 JSON 的 `params` 定义占位符，步骤里用 `{{paramName}}` 引用
- **步骤类型**：`moa_template`（改 MOA 模板字段）、`shell`（执行 shell 命令）

<a id="workflow-cat-1"></a>

## 1) 3周年活动

<a id="anniversary_egg_data_test"></a>

### 3周年砸金蛋数据测试（六步总览）

MSE 金蛋配置 → 账号获次 → 批量砸蛋落表 → 概率保底验收 → 奖品到账验收 → 测试结果汇总。

**默认参数**

- `workbookUrl`：`https://alidocs.dingtalk.com/i/nodes/jb9Y4gmKWr7wodldC4ow9vLPVGXn6lpz`
- `userId`：`100486375`
- `smashCount`：`1`
- `timeoutMs`：`60000`

**钉钉 Sheet**

| Sheet | 内容 |
| --- | --- |
| 金蛋活动配置 | MSE activityConfig.Year3Anniversary：奖池、权重、神秘保底、蛋等级 |
| 砸金蛋测试记录 | 每次 smashEgg 一行：次数、蛋级、奖品、房/用/平计数、验收 verdict |
| 测试结果 | 各步样本数、通过数、失败数与总评 |

**六步顺序**

#### 第  步 · `anniversary-egg-smash-record` → Sheet「（新建整表）」

新测试可用 --create-workbook 在测试目录新建独立表格

**执行操作（按顺序）**

1. anniversary_egg_smash_record.py --create-workbook --parent-folder <nodeId>
2. 预建 Sheet：金蛋活动配置、砸金蛋测试记录
3. 输出 workbookUrl 供后续步骤使用

#### 第 1 步 · `anniversary-egg-mse-to-workbook` → Sheet「金蛋活动配置」

MSE Application/activityConfig.Year3Anniversary → 金蛋活动配置

**执行操作（按顺序）**

1. MSE 拉取 activityConfig.Year3Anniversary
2. 解析 storeLotteryId、userGuaranteeMod、roomGuaranteeMod、蛋升级门槛
3. 写入钉钉 Sheet「金蛋活动配置」

#### 第 2 步 · `batch-anchor-egg-chances` → Sheet「砸金蛋测试记录」

主播/手机号账号自送获次，钻石不足补钻

**执行操作（按顺序）**

1. batch_anchor_egg_chances 或 multi_account 前置：解析 userId/房间
2. 自送 giftIds 礼物随机获次 1~15
3. MOA getEggHome 确认剩余次数

#### 第 3 步 · `anniversary-egg-smash-record` → Sheet「砸金蛋测试记录」

smashEgg 逐次 append；多账号可用 multi_account_verify

**执行操作（按顺序）**

1. MOA smashEgg(userId, roomId) 默认自己的房间
2. testGetMysteryCount 写入房/用/平计数
3. append 至 Sheet「砸金蛋测试记录」

#### 第 4 步 · `anniversary-egg-multi-account-verify` → Sheet「砸金蛋测试记录」

对照配置验收权重、神秘保底 mod、蛋等级

**执行操作（按顺序）**

1. 读 Sheet「金蛋活动配置」奖池与保底 mod
2. evaluate_acceptance_verdict 写验收列
3. mystery_count 辅助核对保底触发

#### 第 5 步 · `anniversary-egg-direct-smash-diamond-verify` → Sheet「砸金蛋测试记录」

应发钻/VIP/装扮 vs 账户实发增量

**执行操作（按顺序）**

1. 发奖前后查钻、查 VIP 经验
2. evaluate_diamond_credit 写到账列
3. 更新 verdict

#### 第 6 步 · `anniversary-egg-test-result` → Sheet「测试结果」

汇总六步验收结论（演示页 / 手工汇总）

**执行操作（按顺序）**

1. 统计各步样本与 pass/fail
2. 写入 Sheet「测试结果」
3. 刷新 family_pk_report 抽奖演示

**提示词**

- `执行3周年砸金蛋数据测试 userId=<userId>`
- `测试3周年砸金蛋，第 1 步 · MSE 配置同步入表`

**注意**

- 勿与家族 PK 表混用；默认独立表 jb9Y4gmKWr7wodldC4ow9vLPVGXn6lpz
- 新测试可用 --create-workbook 新建表格
- 批量场景优先 anniversary_egg_multi_account_verify.py

<a id="anniversary_egg_smash_record"></a>

### 3周年砸金蛋-造数并记录

- **功能**：smashEgg：房间默认自己的房间；每砸一次立即写入钉钉「砸金蛋测试记录」（砸一次写一次）。
- **提示词**：
  - `执行工作流 3周年砸金蛋-造数并记录`
  - `工作流 anniversary-egg-smash-record <userId> <smashCount>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run anniversary-egg-smash-record --workbook https://alidocs.dingtalk.com/i/nodes/jb9Y4gmKWr7wodldC4ow9vLPVGXn6lpz --user-id <userId> --smash-count <smashCount>
```

<a id="workflow-cat-2"></a>

## 2) MOA-generative

<a id="moa_generative_run"></a>

### MOA-generative-抓包转MOA执行

- **功能**：将 Tunnel 抓包 body + 调用链 ServiceUrl/Method 生成双写 header+params 的 MOA payload 并执行。默认业务拒绝（已签到/已点赞）也算成功；--strict 要求业务 success。
- **提示词**：
  - `执行工作流 MOA-generative-抓包转MOA执行`
  - `工作流 moa-generative-run <serviceUrl> <method> <bodyFile> <timeoutMs> <payloadOut> <strict>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run moa-generative-run --service-url <serviceUrl> --method <method> --body-file <bodyFile> --timeout-ms <timeoutMs> --payload-out <payloadOut> --strict <strict>
```

<a id="workflow-cat-3"></a>

## 3) 亲密关系

<a id="intimate_cp_form"></a>

### 亲密关系-结CP

- **功能**：测试环境结CP：Gift --intimate-invite 发起（默认 cpGiftList 特价礼 2005006943）→ MOA acceptIntimateInvitation 同意 → intimateHomePage 验收。relationshipType 固定 1；可选先 dismiss。
- **提示词**：
  - `执行工作流 亲密关系-结CP`
  - `工作流 intimate-cp-form <fromUserId> <toUserId> <giftId> <dismissFirst>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run intimate-cp-form --from-user-id <fromUserId> --to-user-id <toUserId> --gift-id <giftId> --dismiss-first <dismissFirst>
```

<a id="intimate_buddy_form"></a>

### 亲密关系-结挚友

- **功能**：测试环境结挚友：Gift --intimate-invite 发起（默认 gift 2005007129）→ MOA acceptIntimateInvitation 同意 → intimateHomePage 验收。可选先 dismiss。relationshipType 默认 2。
- **提示词**：
  - `执行工作流 亲密关系-结挚友`
  - `工作流 intimate-buddy-form <fromUserId> <toUserId> <giftId> <dismissFirst> <relationshipType>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run intimate-buddy-form --from-user-id <fromUserId> --to-user-id <toUserId> --gift-id <giftId> --dismiss-first <dismissFirst> --relationship-type <relationshipType>
```

<a id="workflow-cat-4"></a>

## 4) 家族PK

<a id="family_pk_data_test"></a>

### 家族PK数据测试（新表+六步总览）

新测试先新建钉钉表，再执行六步：参数表 → 家族列表 → 收礼榜与档位 → 匹配验收 → 成员 PK 与贡献榜验收 → 发奖实发对比。

**默认参数**

- `workbookUrl`：``
- `pkDate`：`2026-07-02`
- `rankDate`：`2026-07-01`
- `userId`：`100486375`
- `timeoutMs`：`180000`

**钉钉 Sheet**

| Sheet | 内容 |
| --- | --- |
| 参数表 | MSE familyPkConfig 参数（含 minWinPk、minRewardPk、minDailyAvg 等） |
| 家族列表 | 家族 ID/名称/成员 userId/族长；已过滤族长非 MENA 的家族 |
| 家族PK档位 | 收礼榜名次、匹配区间、档位达标 PK 与钻石 |
| 匹配验收 | MOA getFamilyPkPage 对战与同区间匹配验收 |
| 用户发钻测试 | 成员 PK、应得钻石测算（榜单验收在第六步结算后回写） |
| 发钻实发验收 | 用户信息、应发钻石、实发钻石、验收结果 |

**六步顺序**

#### 第  步 · `family-pk-config-create-workbook` → Sheet「（新建整表）」

新测试必做：在 family_pk_workbook_folder 目录新建 {pkDate}家族PK数据测试

**执行操作（按顺序）**

1. 读取 platform/dingtalk_gateway/config/family_pk_workbook_folder.json 父目录
2. 创建钉钉 WORKBOOK，命名 {pkDate}家族PK数据测试
3. 预建 Sheet：参数表、家族列表、家族PK档位、匹配验收、用户发钻测试、发钻实发验收、测试结果
4. 输出 workbookUrl，供后续步骤 --workbook-url 使用

#### 第 1 步 · `family-pk-config-mse-to-dingtalk` → Sheet「参数表」

MSE voga-common/familyPkConfig → 参数表；mode=merge 可增量更新

**执行操作（按顺序）**

1. MSE 拉取 voga-common / familyPkConfig 最新配置
2. 解析 bracketGradients、minWinPk、minRewardPk、basePoolDiamond 等参数
3. merge 模式：在原「参数表」上更新数值、增删参数行（rebuild 则整表重建）
4. 同步 configValue_JSON Sheet（原始 JSON 备份）
5. 钉钉文档重命名为 {pkDate}家族PK数据测试

#### 第 2 步 · `family-pk-config-pk-list-to-dingtalk` → Sheet「家族列表」

开发者后台拉家族与成员；族长大区非 MENA 的家族剔除

**执行操作（按顺序）**

1. Admin getAllFamilyList 分页拉取全量家族 ID/名称
2. Admin queryUserDetail 查族长大区，剔除非中东（MENA）族长家族
3. MOA 查询各家族成员 userId 列表
4. Admin queryUserDetail 补全成员手机号
5. 标记是否族长，写入钉钉「家族列表」
6. 钉钉文档重命名为 {pkDate}家族PK数据测试

#### 第 3 步 · `family-pk-config-rank-rematch` → Sheet「家族PK档位」

收礼榜按人数分段随机：>10 人 20 万~100 万，≤10 人 0~20 万；不重匹配

**执行操作（按顺序）**

1. 读「家族列表」统计各家族成员数
2. 按人数分段随机收礼值：>10 人 20 万~100 万；≤10 人 0~20 万（0 不上榜）
3. MOA modifyReceiveDailyRankForTest 批量写入 rankDate 收礼日榜
4. MOA queryReceiveDailyRankForTest 读 rankDate 收礼榜（排除已解散家族）
5. 按参数表 bracketGradients 计算各家族档位达标 PK 与钻石
6. 写入钉钉「家族PK档位」

#### 第 4 步 · `family-pk-config-match-verify` → Sheet「匹配验收」

清除并重匹配 pkDate；MOA getFamilyPkPage 拉取对战列表验收

**执行操作（按顺序）**

1. MOA clearMatchDataForTest 清除 pkDate 匹配数据
2. MOA runFamilyPkMatchTask(pkDate) 重新匹配家族 PK
3. MOA getFamilyPkPage 拉取 pkDate 对战列表（pkList）
4. 读 rankDate 收礼榜，按 bracketGradients 验收同区间匹配
5. 写入钉钉「匹配验收」（对战、区间、验收结果）

#### 第 5 步 · `family-pk-config-member-pk-reward` → Sheet「用户发钻测试」

清 PK → 全员随机造数 → 纠偏边界 → 测算应发钻（榜单验收在第六步结算后）

**执行操作（按顺序）**

1. MOA clearAllFamilyPkScoreForTest 清除 pkDate 全部成员 PK
2. 读「家族列表」全员；读参数表 minWinPk / minRewardPk
3. 全员随机 incrFamilyPkScoreForTest（0~maxScore，默认 5 万）
4. 读「匹配验收」选 1 组平局 + 1 组双方低于 minWinPk（优先小家族）纠偏 PK
5. 按对战结果与奖池规则计算各成员应得钻石，写入「用户发钻测试」

#### 第 6 步 · `family-pk-config-dispatch-verify` → Sheet「发钻实发验收 / 用户发钻测试」

清结算 → 发奖 → 实发查钻验收 → MOA 贡献榜回写用户发钻测试

**执行操作（按顺序）**

1. 读测算结果汇总应得钻石与用户列表
2. MOA resetSettleDataForTest(pkDate) 清除结算发奖记录
3. MOA 批量查钻记录发奖前 baseline
4. MOA runFamilyPkMatchTask(pkDate+1) 下发 pkDate 奖励
5. MOA 批量查钻记录发奖后余额
6. 对比应得 vs 实际增量，写入「发钻实发验收」
7. 逐家族 MOA getFamilyPkUserList，榜单PK/榜单钻/榜单验收回写「用户发钻测试」

#### 第 7 步 · `family-pk-config-test-result` → Sheet「测试结果」

汇总六步验收数据，输出总体验收结论与发钻不一致明细

**执行操作（按顺序）**

1. 读取各步骤 Sheet 摘要行与验收列统计
2. 汇总步骤 0~6 通过/失败/样本数
3. 写入「测试结果」Sheet

**提示词**

- `执行家族PK数据测试工作流 pkDate=<pkDate>`
- `家族PK数据测试 六步 rankDate=<rankDate> pkDate=<pkDate>`
- `重跑家族PK第4-5步`

**注意**

- 新测试须先执行第 0 步新建钉钉表，勿复用历史 workbookUrl
- 钉钉表命名：{pkDate}家族PK数据测试
- rankDate 默认为 pkDate 前一日；收礼榜造数写在 rankDate
- 收礼榜或参数变更后建议重跑第 3~6 步；仅重匹配后重跑第 4~5 步
- 第四步验收使用 MOA getFamilyPkPage（request.date=pkDate），无需 Tunnel 抓包与 App 刷新
- 第五步仅测算应得钻石写入「用户发钻测试」
- 第六步发奖后 MOA 贡献榜验收回写「用户发钻测试」榜单列，并写入「发钻实发验收」

<a id="family_pk_config_create_workbook"></a>

### 家族PK配置-新建测试钉钉表

- **功能**：第 0 步（新测试必做）：在配置的 alidocs 目录新建钉钉表格 {pkDate}家族PK数据测试，预建各步骤 Sheet；输出 workbookUrl 供后续步骤使用。
- **提示词**：
  - `新建家族PK测试钉钉表 pkDate=<pkDate>`
  - `工作流 family-pk-config-create-workbook <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-create-workbook --pk-date <pkDate>
```

<a id="family_pk_config_mse_to_dingtalk"></a>

### 家族PK配置-MSE同步到钉钉参数表

- **功能**：第一步：从 MSE voga-common 读取 familyPkConfig 最新配置，整理为参数表写入 Sheet「参数表」。merge 模式可在原表上更新数值、增删参数行。
- **提示词**：
  - `执行工作流 家族PK配置-MSE同步到钉钉参数表`
  - `工作流 family-pk-config-mse-to-dingtalk <workbookUrl> <pkDate> <namespace> <configKey> <paramSheet> <mode>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-mse-to-dingtalk --workbook-url <workbookUrl> --pk-date <pkDate> --namespace <namespace> --config-key <configKey> --param-sheet <paramSheet> --mode <mode>
```

<a id="family_pk_config_pk_list_to_dingtalk"></a>

### 家族PK配置-后台家族成员写入钉钉

- **功能**：第二步：从开发者后台 getAllFamilyList 拉取全部家族 ID/名称，补全成员 userId、手机号并标记族长；族长大区非中东（MENA）的家族剔除后写入 Sheet「家族列表」。
- **提示词**：
  - `执行工作流 家族PK配置-后台家族成员写入钉钉`
  - `工作流 family-pk-config-pk-list-to-dingtalk <workbookUrl> <sheetName> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-pk-list-to-dingtalk --workbook-url <workbookUrl> --sheet-name <sheetName> --pk-date <pkDate>
```

<a id="family_pk_config_rank_rematch"></a>

### 家族PK配置-收礼榜造数与档位测算

- **功能**：第三步：按 Sheet「家族列表」成员数分段随机写入 rankDate 收礼榜（>10 人：20 万~100 万；≤10 人：0~20 万），再按收礼榜+参数表计算 pkDate 各家族档位达标 PK 与钻石，写入 Sheet「家族PK档位」。不重匹配。
- **提示词**：
  - `执行工作流 家族PK配置-收礼榜造数与档位测算`
  - `工作流 family-pk-config-rank-rematch <workbookUrl> <rankDate> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-rank-rematch --workbook-url <workbookUrl> --rank-date <rankDate> --pk-date <pkDate>
```

<a id="family_pk_config_match_verify"></a>

### 家族PK配置-重匹配与验收写入钉钉

- **功能**：第四步：清除并重匹配 pkDate 家族 PK → MOA getFamilyPkPage 拉取对战列表 → 按收礼榜区间验收同区间匹配，写入 Sheet「匹配验收」。
- **提示词**：
  - `执行工作流 家族PK配置-重匹配与验收写入钉钉`
  - `工作流 family-pk-config-match-verify <workbookUrl> <userId> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-match-verify --workbook-url <workbookUrl> --user-id <userId> --pk-date <pkDate>
```

<a id="family_pk_config_member_pk_reward"></a>

### 家族PK配置-成员PK造数与发钻测算

- **功能**：第五步：清除 pkDate 全部成员 PK → 全员随机 PK → 纠偏边界场景 → 测算应得钻石写入「用户发钻测试」（榜单验收在第六步结算后）。
- **提示词**：
  - `执行工作流 家族PK配置-成员PK造数与发钻测算`
  - `工作流 family-pk-config-member-pk-reward <workbookUrl> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-member-pk-reward --workbook-url <workbookUrl> --pk-date <pkDate>
```

<a id="family_pk_config_dispatch_verify"></a>

### 家族PK配置-发奖与验收

- **功能**：第六步：resetSettleDataForTest 清结算 → runFamilyPkMatchTask(次日) 发奖 → 查钻对比应发 vs 实发写入「发钻实发验收」→ MOA 贡献榜验收回写「用户发钻测试」。
- **提示词**：
  - `执行工作流 家族PK配置-发奖与验收`
  - `工作流 family-pk-config-dispatch-verify <workbookUrl> <pkDate> <userId>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-dispatch-verify --workbook-url <workbookUrl> --pk-date <pkDate> --user-id <userId>
```

<a id="family_pk_config_test_result"></a>

### 家族PK配置-测试结果汇总

- **功能**：第七步：读取各步骤 Sheet 验收摘要，汇总写入「测试结果」（总体验收结论 + 发钻不一致明细）。
- **提示词**：
  - `执行工作流 家族PK配置-测试结果汇总`
  - `工作流 family-pk-config-test-result <workbookUrl> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-test-result --workbook-url <workbookUrl> --pk-date <pkDate> --user-id <userId>
```

<a id="family_pk_config_reward_calc"></a>

### 家族PK配置-档位PK与奖励测算

- **功能**：辅助：仅按收礼榜与参数表重算各家族档位达标 PK 与钻石，写入 Sheet「家族PK档位」（第三步已内置，可单独重跑）。
- **提示词**：
  - `执行工作流 家族PK配置-档位PK与奖励测算`
  - `工作流 family-pk-config-reward-calc <workbookUrl> <rankDate> <pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-reward-calc --workbook-url <workbookUrl> --rank-date <rankDate> --pk-date <pkDate>
```

<a id="family_pk_config_sheet_to_json"></a>

### 家族PK配置-钉钉参数表生成JSON

- **功能**：辅助：读取 Sheet「参数表」中已修改的数值，合并回 MSE 原始结构，写回 configValue_JSON Sheet。改参后执行；勿直接声称已发布到 MSE。
- **提示词**：
  - `执行工作流 家族PK配置-钉钉参数表生成JSON`
  - `工作流 family-pk-config-sheet-to-json <workbookUrl>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-config-sheet-to-json --workbook-url <workbookUrl>
```

<a id="family_pk_daily_rematch"></a>

### 家族PK-重置匹配并下发前日奖励

- **功能**：独立：清除 pkDate 匹配数据 → 结算前一日并发奖 → 重新匹配 pkDate。参数 pkDate、timeoutMs（默认 180000）。
- **提示词**：
  - `重置 <pkDate> 家族PK匹配并下发前日奖励`
  - `工作流 family-pk-daily-rematch pkDate=<pkDate>`
  - `家族PK每日重置匹配 pkDate=<pkDate>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run family-pk-daily-rematch --pk-date <pkDate> --timeout-ms <timeoutMs>
```

<a id="workflow-cat-5"></a>

## 5) 房间

<a id="room_member_quick_add_form"></a>

### 房间成员-快速添加

- **功能**：测试环境快速添加房间成员：MOA room-member-stage.apply（申请人）→ agree（房主）。HTTP 对应 /yaahlan/room/member/apply 与 /member/agree。
- **提示词**：
  - `执行工作流 房间成员-快速添加`
  - `工作流 room-member-quick-add-form <applicantUserId> <ownerUserId> <roomId>`
  - `快速添加房间成员`
- **命令**：

```bash
python3 workflow/workflow_execute.py run room-member-quick-add-form --applicant-user-id <applicantUserId> --owner-user-id <ownerUserId> --room-id <roomId>
```

<a id="workflow-cat-6"></a>

## 6) 抓包

<a id="tunnel_capture_common"></a>

### Tunnel 抓包常用验收

按场景查 Tunnel：礼物送礼/面板/背包、家族PK、进房、动态发布、资料保存、弹窗信号。索引 adb/config/tunnel_capture_catalog.json。

**默认参数**

- `userId`：`100007541`
- `sinceSeconds`：`600`

**六步顺序**

#### 第 1 步 · `` → Sheet「」

点 Send 后验收 response.ec=200

#### 第 2 步 · `` → Sheet「」

打开橙色礼物盒后

#### 第 3 步 · `` → Sheet「」

MOA 下发后开面板；bid=baseProductId

#### 第 4 步 · `` → Sheet「」

切日期 tab 并刷新

#### 第 5 步 · `` → Sheet「」

**提示词**

- `抓包常用验收列表`
- `tunnel capture list`
- `执行抓包验收 gift_send userId=<userId>`

**注意**

- 完整目录：python3 adb/adb_execute.py tunnel capture list
- 文档：adb/录制脚本/Tunnel抓包常用验收.md
- 提交表单类须优先抓包，不以 Toast 为准

<a id="workflow-cat-7"></a>

## 7) 背包礼物

<a id="package_gift_backpack_verify"></a>

### 背包礼物-Tunnel背包验收

- **功能**：用户已在 App 打开礼物面板后，Tunnel 抓 getGiftTabListV3 → 背包 Tab 读 package.remain；按 bid 验收数量。propPackageList 仅道具不含礼物。
- **提示词**：
  - `执行工作流 背包礼物-Tunnel背包验收`
  - `工作流 package-gift-backpack-verify <userId> <baseProductId> <expectRemain> <sinceSeconds>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run package-gift-backpack-verify --user-id <userId> --base-product-id <baseProductId> --expect-remain <expectRemain> --since-seconds <sinceSeconds>
```

<a id="package_gift_dispatch_verify"></a>

### 背包礼物-下发与Tunnel验收

- **功能**：MOA addPackageGift 下发 → 用户在 App 打开礼物面板 → Tunnel getGiftTabListV3 背包 Tab 验收 package.remain。两步之间须手动开面板。
- **提示词**：
  - `执行工作流 背包礼物-下发与Tunnel验收`
  - `工作流 package-gift-dispatch-verify <userId> <baseProductId> <num> <sinceSeconds>`
- **命令**：

```bash
python3 workflow/workflow_execute.py run package-gift-dispatch-verify --user-id <userId> --base-product-id <baseProductId> --num <num> --since-seconds <sinceSeconds>
```
